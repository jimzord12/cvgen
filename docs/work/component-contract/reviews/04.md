# Review round 4: component-contract

Snapshot: `refactor/component-contract` at c66c0fb (pushed), in the clean worktree `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-contract`. Paths below are relative to that worktree. I read the whole net change `main...c66c0fb` (main = origin/main = 207e795).

The brief says round 3 reviewed ce7741e, but report 03 reviewed f232f57. This round therefore also covers four commits that had no independent review: 290f8dd, f688eaf, 9e554b9 and the docs-audit merge ce7741e. I read both merges with `git show --remerge-diff`.

Lead lenses: (1) the merge with candidate-validation; (2) whether the whole net change is ready for main.

Coverage:
1. **Wiring:** the examples go through `flagship`, which builds `ctx` once. The private compositions use the legacy names (author evidence at c66c0fb). The documented ctx-first snippets compile.
2. **Correctness:** the engine is unchanged since 81c6057 apart from main's `domain.meta`. I re-read all 32 wrappers against main's signatures and defaults and found no drift.
3. **Integrity:** the branch touches no workflow code, schema, baseline, example or export. The engineer example still matches v11 exactly.
4. **Contracts and privacy:** `validate.py` picks the schema from import paths, so `lib.typ` module imports still select Flagship's schema. No tracked file names a private workspace.
5. **Tests:** both suite additions run: the example-records case, the contract and parity cases, and the core-boundary check, which also covers the three new core files. I confirmed the guards added after round 3 by mutation.
6. **Failure handling:** the `make-ctx`, `spacing` and `caption` asserts name the fix, and parity failures name their cause.
7. **Simplicity:** c66c0fb adds nothing except the README line.
8. **Docs and repo:**
   - All 36 changed files use LF, with no conflict markers.
   - README's 43 matches the run.
   - verification.md items 7-10, both module maps, AGENTS.md and build-a-cv.md agree with the code.
   - Two minor doc gaps: F12 and F13.

Prior findings:
- **F9:** resolved (`docs/conventions.md:29-30`).
- **F10:** resolved. ce7741e follows the checklist hunk by hunk, the interim sentence is replaced, and docs-audit (db03370) reached main first.
- **F11:** addressed. The worktree equals c66c0fb, and the r3d mutations ran on a copy.
- **N4:** applied, and verified below.
- **F1-F8:** still resolved (spot-checked).

## Findings

### F12 Minor: the roadmap still lists the component contract as future work
Anchor: `docs/vision.md:102-103`, compared with item 1 at `:91-92`, which the docs audit marked in 52a5e36.
- **Scenario:** after the merge, a fresh session derives Now/Next from the vision, as `AGENTS.md` and `development.md` ("A fresh session") instruct.
- **Expected:** item 4 says it was opened and done on 2026-09-25, as item 1 does.
- **Actual:** item 4 is unchanged. Only history.md and ADR 0008's Status line say the work landed.
- **Impact:** low. The next session reports a stale "Next", the same drift the docs audit just fixed.
- **Fix:** add one line to item 4 in the integration commit (done 2026-09-25; wrappers tracked in `framework-gaps.md`).

### F13 Minor: architecture.md says the page header takes strings
Anchor: `docs/architecture.md:84` (docs-audit text kept at ce7741e), compared with `packages/cv-engine/core/page.typ:8`, `page-header(ctx, data, caption: none)`.
- **Scenario:** an agent follows the table and writes `cc.page-header(ctx, name, rank, caption)`.
- **Actual:** the compile fails loudly. Only the deprecated flat `page-header` takes strings.
- **Fix:** reword to "the header takes a name and headline (no field data) and a caption".

### N5 Note: merge commit message
c66c0fb keeps git's `# Conflicts:` stub and has no resolution note and no Co-Authored-By line, unlike 07c2a7e and ce7741e. It is pushed and harmless. Put the evidence line on the integration merge instead.

### N6 Note: the failure-reading docs omit the parity messages
`verification.md` ("Reading a failure") and `.claude/skills/verify-cv` do not list the parity messages, the page-count message or the list of uncalled names. Adding them is optional.

## Checks rerun
All outputs are under `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-contract\builds\review-component-contract-r4-20260925-034559\`.
- **Full suite:** `python tests/run.py --output ...\suite`, run in the worktree at c66c0fb with bytecode writing off and TEMP inside the folder.
  - Exit 0, PASS 43.
  - Exact v11 match: raster and text equal on pages 1-2.
  - The tree was clean afterwards.
- **Doc snippets:** `snippets\snippets.py` extracts three snippets verbatim: build-a-cv.md section 8, skills-component.md and the conventions.md import line.
  - All three compile.
  - I inspected `guide8-p1.png`. The footer shows the snippet's JANE DOE brand override.
  - My first harness passed a wrong contacts type, which was my error; I corrected it and reran.
- **Mutations:** `mut\mutate_r4.py` runs on a `git archive c66c0fb` copy. It executes the parity block taken verbatim from the copy's own `tests/run.py`. Results are in `mut\output.txt`:
  - The unmutated baseline passes.
  - A legacy `decoration` that drops `artifact` is caught by the artifact-tag check.
  - Both page-backgrounds blank on tinted paper are caught ("page-background drew nothing").
  - An empty legacy page-background is caught by the new page-count message.
  - The copy was restored; file hashes match.
- **Private check:** not rerun, per the brief.

## Evidence inspected
- **At c66c0fb:**
  - Code: the diffs, both remerge diffs, `tests/run.py`, `lib.typ`, and the core and Flagship components, `legacy.typ` and `flagship.typ`.
  - The docs listed under Coverage, plus ADRs 0002 and 0008 and `vision.md`.
  - Workflow and CI: `validate.py`, `render.py` `IMPORT` and `verify.yml`.
- **Author evidence:**
  - `builds\tests-20260925-034138-624219`: 43 cases passed. The workflow's `render.json` records engine c66c0fb with no uncommitted changes.
  - `builds\private-evidence-20260925-034213`:
    - It records head c66c0fb, with no uncommitted engine changes.
    - Both workspaces are equal on pages 1-2.
    - The reference hashes equal round 3's `024021` folder.
  - `builds\mutations-r3d-20260925-030002`.
- **Reports:** 01-03 with their dispositions.

## Limitations
- I could not read the Trello card.
- I did not check the GitHub CI result for c66c0fb (no external access). `development.md` requires it before Done.
- I did not open `private/`.
- I did not fetch origin/main; the local ref is 207e795.

## Verdict: PASS

## Disposition (lead, 2026-09-25)
- F12: vision.md item 4 marked opened and done 2026-09-25, naming the two
  modules and the deprecated wrappers tracked in `framework-gaps.md`.
- F13: architecture.md says the header takes a name and headline (no field
  data) and a caption.
- N5: the integration merge carries the evidence and attribution lines.
- N6: not applied; the parity messages name their cause in the suite output,
  which is enough for now.
- Doc-only fixes after a PASS; no further round. CI is checked on the
  integration merge before the card moves to Done.
