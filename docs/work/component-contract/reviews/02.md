# Review round 2: component-contract

Snapshot 81c6057 (d286219..81c6057; 422f150..81c6057 regression-checked).
Lenses: F1 public surface, naming and docs; F4 guard strength and F2
evidence. Round 1: F1 partly resolved (remainder F6); F2 fixed and
reproduced; F3 fixed; F4 fixed as asked (gaps in F7); F5 fixed; N1
recorded; N2 addressed. All 32 legacy signatures match base; baseline, v11
PDF and examples untouched; private reference hashes match the live files.

## Findings

### F6 Material: component modules contradict the export naming rule they cite
`conventions.md:23-31`, `flagship/components.typ:1-4` vs the docs-audit
branch's `domains-and-roles.md:108-133` (prefixed `postcard-hero`,
"prefixed over module bindings"). Decide the form before integration.

### F7 Minor: parity check empty for page-background; "called" guard is text-only
`legacy-parity.typ:61`, `run.py:108-113`; claims in `verification.md:53`,
`conventions.md:35-37`. Mutations on git-archive copies: M2 page-background
returning `[]` passes (the clipped strip has no art); M4 `L.rule` in a `//`
comment passes; M3 vessel-row hard-coded to `false` passes (only `false`
exercised); M1 rule ignoring `weight` is caught. Also unexercised:
document-shell title/author (metadata), skills bullet/title, decoration
height/artifact, metric gap.

### F8 Minor: maps omit the two module files
`architecture.md:101-132` and `AGENTS.md:69,76` omit `core/components.typ`
and `templates/flagship/components.typ`; `component.typ` and
`components.typ` differ by one letter.

### N3 Note: eight files edited on both branches
`build-a-cv.md` section 8 on docs-audit adds a `copy:` override this
branch's rewrite lacks; the merged snippet needs both, then a recompile.

## Checks rerun
Suite 41 PASS `builds/tests-20260925-022551-196085`; private check PASS
`builds/review-component-contract-r2-private-20260925-022459`; doc snippets
compile `builds/review-component-contract-r2-docsnippets-20260925-023002`;
mutations `builds/review-component-contract-r2-mutations-20260925-022820`.

## Limitations
Trello not read; private `cv.typ` not opened; no approval receipts in the
private workspaces; the docs-audit naming rule was itself under review.

## Verdict: FINDINGS

## Disposition (author, 2026-09-25)

- **F6 fixed (module form kept, lead decision).** The naming rule on the
  docs-audit branch (c84da39) now says a template's components are one
  module `<template>-components` and the core's are `core-components`;
  domain functions keep prefixed flat names. conventions.md states the
  module rule directly and points to domains-and-roles.md; the aligned text
  arrives with the merge of main.
- **F7 fixed, all four plus the extras.** `legacy-parity.typ` is now five
  pages: page 5 holds only `page-background` with the shell's background
  and footer switched off, and the suite asserts it is not blank; the
  "called" guard strips `//` and `/* */` comments first; `vessel-row` is
  called with `true` and `false`; `document-shell` gets a title and author
  in both branches and the suite compares both PDFs' metadata. Also
  exercised now: skills `title:` and `bullet:`, decoration `height:` and
  `artifact: false`, metric `gap:`. Mutations on the worktree (restored
  after each): page-background drawing nothing, vessel-row ignoring the
  switch, `rule` called only in a comment, document-shell dropping the
  title: all four caught; baseline passes. Script and output:
  `builds/mutations-20260925-023721/`. `artifact: false` changes PDF tagging,
  not pixels; the fixture passes it through but only pixels and metadata
  are compared (claims in the docs say pixels and metadata).
- **F8 fixed.** architecture.md's module map and AGENTS.md's table name
  both `components.typ` files and say which file is what: `component.typ`
  builds `ctx` (`make-ctx`), `components.typ` is the module `lib.typ`
  exports.
- **N3 noted.** At the merge of main, build-a-cv section 8 keeps the
  docs-audit `copy:` override (certificates-subtitle, brand) together with
  this branch's `lib.typ` module usage, and the snippet is recompiled.
