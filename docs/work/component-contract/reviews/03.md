# Review round 3: component-contract

Snapshot: `refactor/component-contract` at f232f57 (81c6057..f232f57 reviewed; 422f150..f232f57 sanity-checked). Worktree `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-contract` is clean; paths below are relative to it. The naming rule was compared at docs-audit c84da39 (named in the brief) and 10c4bc3 (committed 02:46, during this review).
Lead lenses: (1) strength of the F7 guards; (2) F6/F8 wording against the binding decision and the docs-audit naming rule, and the N3 integration plan.
Coverage:
1. Wiring: no `packages/` change since 81c6057. The engineer example is exact against v11 in my suite run. The author's private evidence at f232f57 was checked.
2. Correctness: the engine is unchanged. Page 5 runs page-background's continuation branch; the first-page branch runs through the shell on page 1 in both APIs.
3. Integrity: baseline, v11, examples and exports are unchanged since 422f150. The author's in-place mutations were restored exactly (F11).
4. Contracts/privacy: no schema change. No tracked file names a private workspace.
5. Tests: I ran 15 mutations of my own; 13 were caught, and the 2 misses fall outside what the docs claim (N4).
6. Failure handling: the most likely page-background regression fails on an assert with no message (N4).
7. Simplicity: +10 fixture lines, +6 runner lines; no scope growth.
8. Docs: F8 resolved; F9, F10.

Prior findings: F6 resolved (the module form matches the rule; one wording gap, F9). F7 resolved. F8 resolved. The N3 plan is incomplete (F10).

## Findings
### F9 Minor: conventions.md says every domain prefixes its functions
Anchor: `docs/conventions.md:29-31` vs docs-audit `domains-and-roles.md`, "Naming rule for exports".
- **Scenario:** an agent adds a marine function and follows "domain functions are the ones that take prefixed flat names".
- **Expected:** marine keeps its flat names (`normalize-candidate`); only later domains prefix theirs.
- **Actual:** "later" is dropped.
- **Impact:** low; one inconsistently named `lib.typ` export.
- **Fix:** write "a later domain's functions take prefixed flat names; marine keeps its flat names".

### F10 Minor: the recorded N3 plan covers one of several merge steps
Anchor: `reviews/02.md` N3 disposition; `conventions.md:31`; `flagship/components.typ:2-3`.
- **Scenario:** merge 10c4bc3 into f232f57, simulated with `git merge-file`.
- **Actual:** 6 conflict hunks in 5 files. The plan names only `build-a-cv.md` section 8 (2 hunks). The others:
  - `architecture.md` and `layout-and-pagination.md`: docs-audit's "certificates and education get the whole layout" is obsolete under ctx; keep this branch's text.
  - ADR 0008 Status line: keep both notes.
  - `history.md`: keep both entries, in date order.
- **Also needed:**
  - One edit that merges cleanly: delete 10c4bc3's interim sentence "until that lands on main, Flagship's components are only the flat names above".
  - Merge order: both anchors cite a section that exists only on docs-audit, so docs-audit must reach `main` first.
- **Impact:** low; stale or dangling text on `main`. Unlike docs-audit R5-F1, nothing fails: `conventions.md` states the rule itself, and its example compiles at f232f57.
- **Fix:** record this checklist and the merge order on the card before integrating.

### F11 Minor: the author's mutations rewrote the working checkout
Anchor: `builds/mutations-20260925-023721/mutate.py:48-60`; `docs/review.md`, "Depth and evidence"; constitution rule 2.
- **Actual:** three tracked files were rewritten in place and then restored. PDFs in the `builds/` root were overwritten on each check; `builds/mutation-legacy.pdf` is the M5-mutated render, with no title.
- **Verified harmless this time:** the restored files equal the f232f57 blobs (`git hash-object --no-filters`). The last restore was at 02:37:24; the commit and the suite ran at 02:38:05.
- **Impact:** an interrupted run would leave neutered source for the next commit to capture.
- **Fix:** mutate a `git archive` copy in a fresh `builds/` folder, as this round did.

### N4 Note: guard limits outside the documented claims
- **B14:** a legacy `decoration` that drops `artifact` passes. The page 3 `/Artifact` count is 9 vs 8, so a per-page `read_contents().count(b'/Artifact')` equality check would catch it.
- **B15:** the "called" guard is textual; a call inside a `#let` that is never invoked still counts.
- **B1:** a page-background that draws nothing removes page 5. It fails at `run.py:118` (`len(a) == len(b) == 5`), which has no message. The author's M2 failed with an empty message, so it most likely failed there too.
- **Non-blank test:** `run.py:126` counts distinct byte values. An empty page with a tinted fill passes (tested), so the check only holds while the fixture's paper is white.
- **Fixture header:** `legacy-parity.typ:3-4` says every wrapper is called once; `vessel-row` is now called twice.

## Checks rerun
All under `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-contract\builds\review-component-contract-r3-20260925-024507\`:
- **Full suite** (`python src/tests/run.py` on `git archive f232f57`): exit 0, PASS 41, `src/builds/tests-20260925-024605-350527`. An earlier attempt of mine used `--output` outside the copy's root; the workflow refused it by design (`suite-stdout.txt`), so it is not a snapshot defect.
- **My mutations B0-B15 on that copy** (`mutate_r3.py`, `mutations-output2.txt`, `mutations/`). My first attempt failed to parse and applied nothing (`mutations-output.txt`). Results of the second run:
  - B0 baseline passes.
  - B1: caught by the page count.
  - B2, B3: caught by the non-blank check.
  - B4: pixels, page 5.
  - B5, B6: pixels, page 4 (both vessel-row values).
  - B7, B8: metadata (legacy wrapper and ctx-first).
  - B9 (call in a block comment): caught as uncalled.
  - B10-B13: pixels.
  - B14, B15: not caught.
  - The copy was restored (`hashes-before` equals `hashes-after2`).
- **Merge simulation:** `merge/` (first read of docs-audit) and `merge-10c4bc3/`.
- **conventions.md example:** compiles at f232f57, exit 0 (`src/builds/r3-doc-check/`).
- **Private check:** not rerun. No `packages/` change since 81c6057, where round 2 reran it.

## Evidence inspected
- **f232f57:**
  - Tests and engine: `tests/run.py`, `legacy-parity.typ`, `lib.typ`, `core/{page,legacy,components,primitives}.typ`, `flagship/{legacy,components}.typ`, `components/experience.typ`.
  - Docs: `AGENTS.md`, `docs/{conventions,architecture,framework-gaps,review}.md`, `verification.md`, `build-a-cv.md` section 8, `reviews/01-02.md`.
- **Author evidence:**
  - `builds/tests-20260925-023805-600045`: PASS 41.
  - `builds/mutations-20260925-023721`.
  - `builds/private-evidence-20260925-024021`: head f232f57, no engine changes, both workspaces raster- and text-equal on pages 1-2, recorded reference hashes equal the live files.
- **docs-audit c84da39 and 10c4bc3:** the naming rule, `reviews/05.md`, and b0cde6c (changes `domain.meta` only; no interaction with this branch).

## Limitations
- Trello card not read (no external access), and private `cv.typ` files not opened.
- docs-audit moved to 10c4bc3 mid-review; both revisions were compared.
- This branch's `review.md` caps rounds at 8; docs-audit's caps unattended work at 10. Round 3 is under both.
- I wrote one stray scratch file, `/tmp/a.txt` (public file names only), outside the repository. Deleting it was denied.

## Verdict: PASS

## Disposition (author, 2026-09-25)

- **F9 fixed.** conventions.md now reads "A later domain's functions take
  prefixed flat names; marine keeps its flat names", with the pointer to
  the naming rule.
- **F10 fixed.** The full merge checklist and the merge order (docs-audit
  reaches `main` first) are on the component-contract card; integration
  waits for the lead's "docs-audit is on main".
- **F11 accepted.** From this round on, mutations run only on a
  `git archive` copy in a fresh `builds/` folder; the working checkout is
  never rewritten. The N4 guards below were verified that way: six
  mutations, all caught (`builds/mutations-r3d-20260925-030002/`, script
  and output; earlier attempts r3, r3b, r3c are kept beside it and show
  the tinted-page gap that f688eaf closed).
- **N4 applied.**
  - The page-count assert has a message naming both counts and the likely
    cause (B1).
  - The parity check compares the per-page `/Artifact` count (B14).
  - The page-5 check uses `Pixmap.color_count() > 1` inside a 2pt border,
    so it holds whatever the paper fill: without the border crop a tinted
    empty page still passed, because its anti-aliased edge adds colours
    (mutation N6; fixed in f688eaf).
  - The fixture header says each wrapper is called at least once.
  - B15 (a call inside a never-invoked `#let`) stays a documented limit in
    the fixture header and in verification.md.
