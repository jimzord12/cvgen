# Review round 1: rename-cvgen

Snapshot: branch `chore/rename-cvgen`, head `1cf346e`, base `main` = `3296e91`; one commit, 11 files, +27/-25. Examined through git objects and a pristine `git archive` extraction; the working tree (on another branch) was not touched.
Lead lenses: 8 (repository and documentation), 4 (contracts and privacy).
Coverage:
1. Wiring: nothing reads the package name at runtime. Zero `@local/`/`@preview/` imports in the tree; examples, fixtures, `tests/workflow.py` ENTRY, the guide and the `new-cv` skill import by path. Proven: the four examples compiled from the extraction with `name = "cvgen"`, engineer raster- and text-identical to the frozen v11. `engine_state` records the folder path, `git rev-parse HEAD` and a dirty flag, never the remote or the manifest name.
2. Correctness: no behaviour change; non-doc edits are two `$id` strings, one schema title, and the manifest name/description.
3. Integrity: `check_frozen()` passes on the extracted tree; frozen reference, baseline, `exports/` and `examples/candidates/` absent from the diff.
4. Contracts: both `$id`s change only the host path; every `$ref` is local; no validator or candidate JSON points at the old `$id`. No credentials or private data.
5. Tests: no test pins the package name, repo URL or board name. Author's run bound to the snapshot: six `render.json` files record `engine.commit = 1cf346e`, 38/38 pass, exact equal.
6. Failure handling: `trello.ps1` `Find-Board` matches the exact name among open boards and exits 1 listing them on 0 or >1 hits; a mismatch is loud and cannot route a write to the wrong board.
7. Scope: 11 files, all name or description strings, matching ADR 0011 and the settled list; folder names, frozen file names, `Marine-*` exports, `domain.meta`, `recipes.md:57` and historical docs untouched.
8. Repository: `marine-engineer-cv` remains only in `docs/work/**`; `marine-cv` only in a historical proposal slug; `Marine CV` only in the provenance note, `recipes.md:57`, `domain.typ` meta, `archive/` and history. `.github/workflows/verify.yml` has nothing repository-specific. All 11 blobs LF, no BOM, trailing LF. One Conventional Commit.

Ordering (board rename before merge): correct; the only window is between the PUT and the merge, during which `Find-Board` fails loudly. `PUT boards/IPsBxAwf` changes only the long URL slug; short links stay valid.

## Findings

No Blocking, Material or Minor findings.

### N1 Note: confirm the board name before merging. Disposition: `-Lists 'CVgen'` run right after the PUT; merge only on success.
### N2 Note (outside the repo): the auto-memory note names the board "Marine CV". Disposition: updated after the PUT.
### N3 Note: the `$id` URLs are identifiers, not links; nothing dereferences them. No action.

## Checks rerun
- From the pristine extraction: four examples compiled, `verify.py` exact against the frozen v11 -> passed; `check_frozen()` -> no assertion. Output: `builds/review-rename-cvgen-r1-20260921-023626/`.
- `python tests/run.py` not rerun (working tree on another branch); the author's run at `1cf346e` inspected instead.

## Limitations
Trello not read. `private/` not opened. Linux CI not checked.

## Verdict: PASS

Name-only change: nothing in the tree resolves the project by package name, repo URL or `$id`, and the board is resolved by exact name in one place, which fails loudly rather than misrouting.
