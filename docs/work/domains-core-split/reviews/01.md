# Review round 1: domains-core-split

Snapshot: branch `refactor/domains-core`, head `059c6c4`, base `main` = `195fbab`; one commit, 33 paths. Working tree clean before and after the checks.
Lead lenses: 2 (correctness and edge cases), 5 (tests and visible evidence).
Coverage:
1. Wiring: `lib.typ` still exports every pre-change name plus `merge`, `compose`, `normalize-common`, `validate-common`, `company-months`, `experience-model`, `marine`; every name the guide's section 8 snippet and the fixtures import resolves. Independent proof that nothing moved for the family: `main` extracted with `git archive`, nine entry points compiled there (captain, captain-silver, chief-officer, engineer, engineer-hidden, chief-officer-copy, pagination, content, certificate-continuation) and compared with the HEAD suite renders: raster, whitespace-normalised text and PDF title/author identical on every page. `tests/workflow.py` ENTRY (no `role:`) renders pixel-identical to v11.
2. Correctness: `merge` is deep for dicts, replaces arrays/scalars/functions, value-semantic; on today's flat `copy` it equals the old shallow `+`. `compose` skips `none`. Int ref returns the raw company; dict ref keeps `display-months` from the full company and `continued` from `rows.at(0) > 0`. Totals come from `d.companies`. Validation order changed (common checks precede rank/companies); no asserted message depends on it. `set document(...) if` works: PDFs carry `<NAME> | <RANK> | Marine CV` and `Marine CV Studio`, byte-equal to main's. Core has no `groups`/`ships` reference left.
3. Integrity: facts preserved; `tests/baseline.json` untouched; exact match holds; `check_frozen` passes.
4. Contracts: both schemas unchanged in shape; no example record has a `copy` key, so the `role` case cannot pass on record copy. Fictional data only.
5. Tests: `data.typ` role assertion fails if template<role or domain<role are swapped or the domain and role were combined shallowly; the literal 15-key dictionary fails if the six domain words drop out. `core-model.typ` companies have no `groups`, so any surviving `groups`/`ships` read errors; `compose(.., none, ..)` asserted directly. `role.typ` fails if `flagship` drops `role:` or the domain wins over the role. `three-pages` `'5 years' == 2` fails if months come from the sliced groups or `display-months` is dropped. `missing-name` fails if `validate-candidate` stops calling `validate-common`.
6. Failure handling: core error strings verbatim; `check_core_boundary` names file and target.
7. Simplicity: scope matches the ADR 0011 Stage 2 list; `experience-model.totals` is template-facing (N2).
8. Repository: all 33 changed files LF, no BOM; boundary grep empty; AGENTS.md map names files that exist; commit subject 75 characters (N3).

## Findings

### F1 Minor: the guide's section 8 snippet does not compile as written
`docs/guides/build-a-cv.md:150-156`: `normalize-candidate` on a facts record yields `copy: (:)` and the shell's footer reads `candidate.copy.brand` -> `dictionary does not contain key "brand"`. Fix: go through `to-flagship-input` first.
Disposition: fixed; the corrected snippet compiles (`builds/scratch-s8/probe.pdf`).

### F2 Minor: the reference says a role may refine anything; Flagship composes only `copy`
`docs/reference/domains-and-roles.md:22-23`, `docs/architecture.md:37` vs `adapter.typ:23` and `flagship.typ` reading `domain.meta`/`domain.experience` directly. Fix: say so in both places, or compose once in `flagship.typ`.
Disposition: documented (both roles are bare markers by settled decision; composing `meta`/`experience` is the moment a role first needs it).

### F3 Minor: `check_core_boundary` misses code-mode `import` and `#include`
`tests/run.py:18`. Fix: `(?<![\w-])(?:import|include)\s+"([^"]+)"`.
Disposition: fixed; verified against the two current core imports (bare) and three evasive forms (caught).

### F4 Minor: README, verification.md, tech-stack.md and the schema `copy` description lag the code
Disposition: fixed (README eight lines with `role:` and 38 cases; verification fixtures table gains `role.typ`, `core-model.typ`, the boundary check; tech-stack points at the marine module; schema string names domain, role and template).

### N1 Note (pre-existing): `core/page.typ` still reads `candidate.copy.brand` and `candidate.disclosure`; a second domain's template must supply them or bypass `document-shell`; `title`/`author` default to `none` silently. Recorded for the travel-and-tourism proof.
### N2 Note: `experience-model.totals` is not part of the core contract. Disposition: pagination comment says the core uses `count` and `slice` only.
### N3 Note: commit subject 75 characters. Disposition: not amended; next ones shorter.
### N4 Note: `to-flagship-input` accepts any `role` value. Disposition: assert added (`role must be a role node ... or none`).

## Checks rerun
- `python tests/run.py` at `059c6c4`: exit 0, `PASS: 38 compilation cases`; exact reference and workflow exact equal -> `builds/tests-20260921-021849-407314/`.
- Boundary greps empty; `main` tree extracted and nine entry points compared with HEAD renders: identical.
- Two probe compiles under `builds/review-domains-core-split-r1/` (F1, F2); `git ls-files --eol`, BOM/trailing-LF scan.

## Limitations
Windows only; no CI run for `059c6c4` checked. Trello card not read. `private/` not opened.

## Verdict: PASS

No Blocking or Material finding. The core is field-neutral in fact, the composition order is the settled one, each new test fails for a distinct wiring defect, and every rendered output of the family is identical to `main` in pixels, text and metadata. F1-F4 fixed in a follow-up commit without a new round.
