<!-- code-reviewer report, round 1, 2026-09-16, headless invocation (claude -p --agent code-reviewer). Stored unchanged; dispositions are in ../task.md. -->

# Review round 1: dev-setup

Snapshot: `e5f5f2d..4db3583` on `docs/product-direction`, HEAD = `4db3583a06d5…`, working tree clean, single commit, 9 files (all docs/agents, nothing under `src/`, `tests/`, `content/`, `themes/`, `layouts/`, `examples/`).
Lead lenses: 8 (repository and documentation), 1 (product fit and wiring)
Coverage:
1. Product fit and wiring: in depth. `CLAUDE.md` → `AGENTS.md` → `docs/development.md` / `docs/review.md` / `.claude/agents/code-reviewer.md` reachable from four places in `AGENTS.md` (responsibilities bullet, docs table, "Skills and agents", working agreement). This round itself runs under the wrapper's prompt, so in-session discovery now works (see F4).
2. Correctness and edge cases: n/a, no code.
3. Data and document integrity: fixture hash lists before/after are byte-identical (`diff` empty); report A's quoted hashes match `hashes-before.txt`; both reports stored with only a one-line HTML comment header, body unchanged in shape.
4. Contracts, access and privacy: wrapper tool list is `Read, Grep, Glob, Bash, PowerShell`, no `Agent`, matches `review.md` §Reviewer boundaries. Fixtures are fictional and outside the repo.
5. Tests and visible evidence: validator rerun (below). Suite not run; I agree: no product code or fixture changed.
6. Failure handling: wrapper returns INCOMPLETE on missing snapshot/evidence; case A shows the reviewer surfacing missing evidence (A6). Fine.
7. Simplicity and ownership: one home per rule respected; wrapper carries no lens/severity/loop text.
8. Repository and documentation: in depth, line-by-line comparison of the three proposals against the two active files. All eight lenses, severity table, verdicts, loop, eight-round cap, brief contents, boundaries, report/persistence, stage table, guardrails, task template and Ready-vs-Done are carried faithfully. A handful of low-consequence sentences were dropped (F1). All changed files are LF in the index.

## Findings

### F1 Minor: a few approved sentences have no active home
Anchor: `docs/development.md` §A fresh session, §Guardrails, §The task record
Dropped from the proposals and not present in any active file (grep confirms): development-protocol.md L58-60 ("Use the briefing on fresh/resumed sessions **and status requests**. Otherwise, answer briefly…"); work-records.md L28-29 ("A saved report … is a dated snapshot rather than an authority"); work-records.md L98-100 ("Cleanup must preserve the evidence still needed by active work … and follow the existing confirmation checkpoint"); work-records.md L82 ("Process authorization records remain separate from task progress"). The owner-managed-Git override (dev-protocol L105) lives only in `AGENTS.md` and `git-workflow.md`, which is acceptable since `development.md` links the latter. Impact: low; none changes behaviour materially, but acceptance box 1 says "every active rule". Fix: add three short sentences to `development.md` (status-request trigger in §A fresh session; dated-snapshot and cleanup-preserves-evidence in §The task record).

### F2 Minor: `AGENTS.md` working agreement reads as requiring the suite for every non-trivial change
Anchor: `AGENTS.md` §Working agreement, bullet 4
Scenario: this task is non-trivial (many files, changes process behaviour) and correctly skipped `python tests/run.py`. `review.md` scopes the suite to "non-trivial implementation changes"; `AGENTS.md` says "every non-trivial change ends with `python tests/run.py` passing". A literal reader sees a contradiction on docs-only tasks. Fix: "Every non-trivial change to code, fixtures or inputs ends with…" or point to `review.md`.

### F3 Minor: wrapper pins `model: opus` / `effort: max`, which `review.md` neither states nor forbids
Anchor: `.claude/agents/code-reviewer.md:5-6`; proposal review-protocol.md L146-147
The proposal explicitly left model/effort unmandated; the wrapper makes that choice silently. Not a contradiction, and consistent with the owner's global reviewer preference, but it is configuration a reader of `review.md` cannot see. Fix: one clause in `review.md` §Reviewer boundaries ("the wrapper pins Opus at maximum effort") or a comment line in the wrapper referencing the choice.

### F4 Minor: acceptance box 3 is ticked while the Handoff says in-session invocation is still unconfirmed
Anchor: `docs/work/dev-setup/task.md:27-30` vs `:98-100`
`review.md` prescribes the Agent tool path; evidence so far was headless `claude -p --agent`. This round was invoked with the wrapper's system prompt, so the Agent-tool path evidently works after restart. Fix: record this round as the in-session confirmation and close the Handoff item.

### F5 Note: "enforced by the reviewer's instructions"
Anchor: `docs/review.md:93-94`
Proposal L92-93 asked not to claim Markdown instructions enforce isolation. The sentence does say "not a security sandbox", but "enforced by" is the word the proposal warned against; "relied on" is more honest.

### F6 Note: sandbox rejects compound shell commands for the reviewer
Observed by both headless runs (O1) and by me in this round (`&&`, `;`, `$?` all refused). Single commands such as `python tests/run.py` still work and write under `builds/` themselves, so the protocol holds; worth keeping O1 on the observation list.

## Checks rerun
- `claude plugin validate .claude/agents/` — "Validation passed" (single command; no output under `builds/`, validator writes nothing).
- `diff evidence/hashes-before.txt evidence/hashes-after.txt` — empty, exit 0.
- `git ls-files --eol` on the nine changed files — all `i/lf`.

## Evidence inspected
`docs/review.md`, `docs/development.md`, `.claude/agents/code-reviewer.md`, `AGENTS.md`, `CLAUDE.md`, `docs/work/dev-setup/task.md`, `docs/work/dev-setup/evidence/*` (all at `4db3583`); `docs/proposals/{development-protocol,review-protocol,work-records}.md`, `docs/proposals/README.md`, `docs/git-workflow.md` (unchanged in range, read at HEAD).

## Limitations
Could not chain commands or persist output under `builds/`; every check above was a single command whose result I read directly. The fixtures themselves are not in the repo, so "reviewer edited nothing" rests on the author's two hash lists plus report A's matching quoted hashes.

## Verdict: PASS

No Blocking or Material findings. F1-F4 are small text fixes the author can make in the same pass that marks the proposals `applied`; they do not need another round unless the author changes `review.md` boundaries or the wrapper's tools.
