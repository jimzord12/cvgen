# Review round 1: docs-audit
Snapshot: 422f150..52a5e36 on docs/audit-2026-09-25. Lead lenses: (1) docs-vs-code correctness incl. P-H1; (2) governance consistency and safety. Coverage 1-8 checked; links/anchors in 32 changed .md files resolve; all LF; git diff --check clean.
### R1-F1 Material: naming-rule example leaves an export unbound — docs/reference/domains-and-roles.md:115-116. An import list without parentheses ends at the newline; line 116 becomes markup and tourism-validate-candidate is never bound (probe B: unknown variable, exit 1; in parentheses it binds, probe C). Fix: wrap the list in ( … ) as build-a-cv.md:177 does.
### R1-F2 Material: the "fictional"/"illustrative" check misses two of three defaults — build-a-cv.md:80-81, new-cv SKILL.md:53-55. Disclosure default is upper case (core/data.typ:23), subtitle default starts "Illustrative", brand default FLAGSHIP is never searched; a captain render without disclosure passes the check though "Illustrative register" prints. Fix: search case-insensitively for fictional, illustrative and flagship, or the three default strings.
### R1-F3 Minor: section 8 never shows where copy goes — build-a-cv.md:61-68,176-189. Candidate schema forbids copy in candidate.json; on the §8 path the only place is to-flagship-input(..., copy: (...)) (adapter.typ:21); probe D prints FLAGSHIP and the Illustrative subtitle. Fix: add copy: to that snippet.
### R1-F4 Minor: the CI command can confirm the previous commit's run — git-workflow.md:69-71. `gh run list --branch main --limit 1` right after a push may return the prior run. Fix: `gh run list --commit <pushed sha> --limit 1`, repeat until listed, then watch; also covers feature branches.
### R1-F5 Minor: owner-reserved list imprecise — preferences.md:50-55, development.md:159. (a) "overwriting real candidate files in private/" covers routine edits the guide requires; (b) "(neither can be recovered from Git)" false for tracked exports/; (c) moving an archive tag = deleting; (d) add: clear builds/ by path, never git clean -X/-x. development.md:159 "unresolved architecture" vs :153 agents choose technical design. Tag cleanup missing from the one list.
### R1-F6 Minor: ADR 0011 body edited — 0011:44-45; decisions/README.md:4-6 says only Status changes after acceptance. Restore; record in Status. ADR 0010 Status lacks "Amended by 0011".
### R1-F7 Minor: checklist over-credits the boundary check — domains-and-roles.md:97-99. Boundary check only asserts core imports are bare siblings. Fix: separate step with empty `git diff --stat <base> -- packages/cv-engine/core packages/cv-engine/domains/marine`.
### R1-N1 Note: naming-rule decision stands (probe A: later duplicate import silently wins). Convention only; when the second domain lands add a run.py check that no name is bound twice in lib.typ.
### R1-N2 Note: constitution section 1 now unfreezes chief-officer-example.json (factually right, never pinned since d0978b2) but rule changes are the owner's; mention in owner summary.
### R1-N3 Note: page-plan error table lacks two messages (pagination.typ:13,42); after merge exports/ metadata differs from code (owner parked).
Checks rerun: probes A-D via typst eval (A 0, B 1, C 0, D 0); pymupdf reads of builds/tests-20260925-013520-233467 (metadata "… | CVgen", author CVgen; exports v12 still "Marine CV"); link/anchor scan 0 problems. Suite not rerun (author run inspected). Limitations: Trello card not read; private/ not opened; live CI not observed.
## Verdict: FINDINGS

## Disposition (author, 2026-09-25)

- **R1-F1 fixed.** The naming-rule example wraps the multi-line import list
  in parentheses; a scratch compile of the same shape binds both prefixed
  names (exit 0, output "n v").
- **R1-F2 fixed.** Guide section 2 and the new-cv skill search the PDF text
  case-insensitively for "fictional", "illustrative" and "flagship"; the
  guide gives a one-line `pymupdf` check that prints `[]` when clean.
- **R1-F3 fixed.** The section 8 snippet passes
  `copy: (certificates-subtitle: ..., brand: ...)` to `to-flagship-input`,
  with a comment that `copy` is not allowed in `candidate.json`.
- **R1-F4 fixed.** git-workflow.md observes CI with
  `gh run list --commit <pushed sha> --limit 1`, repeated until listed, then
  `gh run watch <run-id> --exit-status`.
- **R1-F5 fixed per lead decision.** preferences.md: routine now includes
  editing a workspace's `candidate.json`/`cv.typ` for a CV the owner asked
  for, deleting non-`archive/*` tags, and clearing `builds/` by path (never
  `git clean -x`/`-X`); reserved now reads "deleting or moving `archive/*`
  tags", "deleting anything under `private/` or overwriting or removing its
  revisions, approval receipts and exports", and the root `exports/` PDFs as
  the public released deliverables. Mirrored in AGENTS.md and
  git-workflow.md. development.md now says "architecture outside an
  approved item or an operation reserved for the owner".
- **R1-F6 fixed.** ADR 0011's body sentence restored verbatim; the
  implementation (four stage commits, `core/node.typ` helpers) is recorded
  in its Status line. ADR 0010's Status line already says "Amended by 0011"
  (lines 8-9); the index row gained it in `52a5e36`.
- **R1-F7 fixed.** The wiring checklist says what the core-boundary check
  proves and adds a separate step: an empty
  `git diff --stat <base> -- packages/cv-engine/core packages/cv-engine/domains/marine`.
- **R1-N1 applied.** domains-and-roles.md notes that the suite should check
  for names bound twice in `lib.typ` when the second domain lands.
- **R1-N2 noted** for the owner summary (coordinator).
- **R1-N3 applied.** The page-plan table adds `Row range must contain two
  integer indices` and `Experience requires at least one vessel`. The
  exports/ metadata stays parked with the owner.
