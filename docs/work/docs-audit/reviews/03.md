# Review round 3: docs-audit

Snapshot: 623e464..c123394 on `docs/audit-2026-09-25`, clean tree. Lead
lenses: R2 fixes correct and governance docs agree with `preferences.md`;
the error-table row against `core/pagination.typ`. R2-F1 and R2-N2
resolved; R2-F2 resolved in `preferences.md` but its AGENTS.md mirror
introduced R3-F1; R2-N1 resolved in the guide but the skill mirror drops
the condition (R3-F2). `git-workflow.md`, `development.md`,
`constitution.md` and `history.md` agree with `preferences.md`.

## Findings

### R3-F1 Material: AGENTS.md's reserved-list summary contradicts preferences.md
`AGENTS.md:189-190` vs `preferences.md:36-37,50-51,57-60`. The summary
reserves any overwrite in `private/` beyond candidate.json and cv.typ
(stricter: blocks updating the workspace README or presentation.json that
the guide and skill require) and reads as freeing deletion of those two
files (looser); `.local/` missing. Fix: link instead of enumerating, or
mirror exactly; optionally add presentation.json and README.md to the
routine item.

### R3-F2 Minor: the skill drops the exception's footer condition
`new-cv/SKILL.md:54-57` vs `build-a-cv.md:80-85`. Add "only once the footer
shows the configured brand".

### R3-N1 Note: step 8 assumes local main is what was merged
Say `origin/main` if that is what was merged. Fails safe.

### R3-N2 Note: the row's fix is the first of two steps
Append "and list it in `pages`".

## Checks rerun
CR bytes in changed blobs 0; `git diff --check` exit 0; `typst eval` probes
P1-P3 (no companies / empty groups -> "Experience requires at least one
vessel"; one ship with an empty plan -> "must cover each vessel row once");
three-dot diff behaves as described. Suite not rerun (markdown only).

## Limitations
Trello not read; cannot confirm 02.md is the verbatim report (it is a
faithful condensation by the lead); private/ not opened; CI not observed.

## Verdict: FINDINGS

## Disposition (lead, 2026-09-25)
R3-F1: AGENTS.md no longer enumerates the reserved list; it points to
preferences.md as the only source. preferences.md adds `presentation.json`
and the workspace `README.md` to the routine edits. R3-F2, R3-N1, R3-N2
applied as suggested.
