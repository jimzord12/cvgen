# Review round 6: docs-audit

Snapshot: `c84da39..10c4bc3` and the whole branch `422f150..10c4bc3` (10 commits, 39 files) on `docs/audit-2026-09-25`, clean tree, HEAD 10c4bc3. Paths are relative to `C:\Users\jimzord12\Documents\GitHub\cvgen`. `main` is at 422f150, which is the merge base (local refs; I did not fetch). For comparison I read `refactor/component-contract` at its committed head f232f57, from git objects only.

Lead lenses: (1) R5-F1 and R5-N1..N3 are resolved, and every naming-rule statement matches `lib.typ` at this snapshot; (2) the whole branch is ready to merge to `main`.

Coverage:
1. **Wiring:** the flat names the rule cites exist (Q1). `flagship-components` and `core-components` do not exist (Q2-Q4), and `domains-and-roles.md:136-138` now says so. The guide's `copy:` and `disclosure` path traces through `flagship.typ:16-19`, `adapter.typ:21-25`, `core/data.typ:23` and `page.typ:12-14`, and `""` does blank a slot.
2. **Correctness:** a later duplicate import wins silently, in both orders (Q5). The example's `as` renames inside parentheses bind both names (Q6). Every quoted error string exists (`pagination.typ:9-49`, `verify.py:59`, `run.py:19`, `workflow.py:49`), and so do the four REFUSED reasons (`cv.py:66-68`).
3. **Integrity:** the only code change is b0cde6c (PDF metadata); fresh renders read "… | CVgen". The `exports/` mismatch this creates is covered by `git-workflow.md:23-26`.
4. **Contracts and privacy:** no schema changed. A scan of the diff found no personal data.
5. **Tests:** the suite passes on rerun at 10c4bc3. Constitution section 1's frozen set matches `baseline.json` exactly (31 entries), and no changed file is pinned.
6. **Failure handling:** n/a for docs. The documented failures match the code (lens 2).
7. **Ownership:** the rule describes what exists, and the future part is marked and owned by a card. `findings.md:136-140` records the revision.
8. **Repository and docs:**
   - Diff check clean, 0 CR bytes, and 100 relative links across 37 files with 0 broken links or anchors.
   - `preferences.md` holds the only list of reserved operations, and the governance docs defer to it.
   - Old rules survive only in retired or proposal files, plus constitution line 13 (resolved in R4).
   - P-M7 (`README.md:60` calls the schema a validation) predates this branch and is recorded as deferred to candidate-validation.

Lens 1 result: every statement in `domains-and-roles.md:108-141` holds at this snapshot. Lines 130-136 name the two modules in the present tense, but the next sentence says they come later, which is enough. R5-F1, R5-N1 and R5-N2 are resolved (`:115`, `:126-128`, `lib.typ:12`). R5-N3 is resolved for 02-04 (see R6-N2).

## Findings

### R6-N1 Note: the later contract merge must keep this branch's section 8 fix
Anchor: `docs/guides/build-a-cv.md:193-194`; `domains-and-roles.md:136-138`

**Scenario:** this branch merges first, then f232f57. An in-memory trial merge conflicts in five files: `architecture.md`, ADR 0008, `build-a-cv.md`, `history.md` and `layout-and-pagination.md`.

**Risk:** the section 8 snippet at f232f57 calls `to-flagship-input(json("candidate.json"))` with no `copy:` (its line 156). If the merge takes that side, the R1-F3 fix is lost without any warning. A real CV built through section 8 would then print "FLAGSHIP" and the "Illustrative register … fictional" subtitle. Separately, the contract's `ctx` wording makes this branch's "whole layout" sentences out of date (`architecture.md:52-53`, `layout-and-pagination.md:14-15`).

**Fix:** add these steps to the `component-contract` card, next to removing the clause:
- move `copy:` into the ctx-first snippet;
- keep both `history.md` entries;
- drop the "whole layout" sentences;
- rerun the link scan.

### R6-N2 Note (investigation item): 01.md has no record note
01.md is as condensed as 02-04: it has no per-lens coverage and no Evidence inspected section. The notes in 02-04 say "Rounds from 05 are stored verbatim", which implies 01 is not verbatim either. Label 01.md, or state that it is verbatim.

### R6-N3 Note (out of scope, pre-existing): `lib.typ` exports shadow Typst built-ins
`lib.typ:6` exports `label` and `duration`. Importing them replaces Typst's built-ins of the same name (Q7: `label == std.label` is false). No tracked file imports `lib.typ` with a wildcard. The future duplicate-name check (`domains-and-roles.md:139-141`) could also flag built-in names.

## Checks rerun
- `python tests/run.py` at 10c4bc3: exit 0, PASS, 38 cases, exact reference passed. Output is in `builds/tests-20260925-025450-946461/`, and the tree was clean afterwards.
- `typst eval --in -` (Typst 0.15.1), run in memory:
  - Q1: seven flat names import; exit 0.
  - Q2/Q3: importing the two modules fails with "unresolved import"; exit 1.
  - Q4: a wildcard import binds `hero` (exit 0) but neither module (exit 1 each).
  - Q5: of two `theme` imports, the second wins with no warning.
  - Q6: the example syntax binds; exit 0.
  - Q7: `label` and `duration` shadow the built-ins.
- `git diff --check 422f150..10c4bc3`: exit 0. The CR scan, the link scan (script in the session scratchpad) and the baseline pin scan are all clean.
- `git merge-tree --write-tree 10c4bc3 f232f57`: exit 1, 5 conflicts. **Disclosure:** this wrote unreferenced objects into `.git/objects`. It changed no ref, index or worktree, but it falls outside the `builds/` allowance, and I did not repeat it.

## Evidence inspected
- 10c4bc3 docs: the whole diff; `lib.typ`; the naming rule; `preferences.md`, `AGENTS.md`, `architecture.md` and `git-workflow.md`; ADR 0008; `findings.md`; reviews 01-05.
- 10c4bc3 code: Flagship's `flagship.typ`, `adapter.typ` and `hero.typ`; the core's `data.typ`, `page.typ` and `pagination.typ`; `tests/*.py`; `baseline.json`; `cv.py`; the `cv_workflow` raise sites.
- f232f57, from git objects: `lib.typ`, `conventions.md`, `AGENTS.md`, `architecture.md`, `build-a-cv.md`, `history.md`, `flagship.typ`.
- The author's report in `builds/tests-20260925-013520-233467`: it records no revision. It started 5 s after b0cde6c, and only markdown and `.gitignore` have changed since.

## Limitations
- Trello not read, so I have not seen the card's acceptance criteria, the handoff note on the `exports/` mismatch, or the `component-contract` card.
- I cannot verify that the owner gave a written go for the constitution section 1 wording (R1-N2).
- Remote not fetched; `private/` not opened; CI not observed.

## Verdict: PASS

## Disposition (lead, 2026-09-25)
- R6-N1: the component-contract author has the full merge checklist
  (section 8 `copy:` kept in the ctx-first snippet, both history entries,
  "whole layout" sentences dropped, interim clause removed, link scan).
- R6-N2: 01.md labelled as a condensation.
- R6-N3: recorded in findings.md for the future duplicate-name check.
- R1-N2 (constitution section 1 now states that
  `chief-officer-example.json` is not pinned): a factual correction of what
  the baseline has pinned since 2026-09-16, reported to the owner in the
  morning summary.
