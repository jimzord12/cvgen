# Review round 2: docs-audit

Snapshot: 623e464 on `docs/audit-2026-09-25` (fixes 52a5e36..623e464; whole
range 422f150..623e464 sanity-checked). Lead lenses: round-1 resolution;
governance consistency of the owner-reserved list. 33 .md files, 99
relative links, 0 broken; all LF; `git diff --check` clean; ADR 0011
differs from 422f150 only by three added Status lines. Round-1 F1, F2, F3,
F4, F6, F7, N1 resolved; F5 resolved as the lead decided with one gap
(R2-F2); the N3 fix introduced R2-F1.

## Findings

### R2-F1 Minor: new error-table row prescribes a fix that cannot work
`layout-and-pagination.md:85`, `core/pagination.typ:38-42`. The message
appears only when the candidate has no vessel rows (probes P1-P3). Fix:
meaning "The candidate has no vessel rows"; fix "Add at least one company
with a vessel to candidate.json".

### R2-F2 Minor: overwriting un-snapshotted private files is unlisted
`preferences.md:57-59`, `AGENTS.md:189`. Portrait, `sources/` originals,
`presentation.json` and `.local/` are neither routine nor reserved;
revisions snapshot only cv.typ, candidate.json and the portrait. Fix: add
overwriting portrait and source documents (and `.local/`) to the reserved
item; AGENTS.md "deletions or overwrites in private/".

### R2-N1 Note: "flagship" is ordinary marine wording
The check could push an agent to edit the candidate's own words; add the
exception once the footer shows the configured brand.

### R2-N2 Note: checklist step 8 after merging main
Use `git diff --stat main...HEAD -- ...` to avoid a false alarm.

### R2-N3 Note (out of scope): `presentation.json` is not in the revision snapshot
`build-a-cv.md:16,205-209`, `cv_workflow/render.py:41-56`. A relative
`json("presentation.json")` would fail inside `revisions/<id>/inputs/`; a
root-absolute one reads the live file, so the revision is not
self-contained. Pre-existing at 422f150; code reading only.

## Checks rerun
`typst eval` probes (F1 fixed import exits 0; section 8 snippet exits 0
with brand and subtitle overridden; P1-P3); the guide's one-liner on
`builds/tests-20260925-013520-233467` detects each default on its own;
`gh run list --commit` and `gh run watch --exit-status` exist; diff check
and link scan clean. Suite not rerun (only .gitignore changed since b0cde6c).

## Limitations
Trello not read; private/ not opened; live CI not observed; whether the
owner has seen the constitution section 1 wording (R1-N2) not verifiable.

## Verdict: PASS

## Disposition (lead, 2026-09-25)
R2-F1, R2-F2 (portrait, source documents and `.local/` reserved; AGENTS.md
"deletions or overwrites in private/ beyond a workspace's candidate.json and
cv.typ"), R2-N1 and R2-N2 applied. R2-N3 is a real workflow gap for
custom compositions that read sibling data files; recorded as a Queued
card (`revision-snapshot`) rather than fixed here, since it is code and out
of this docs task's scope.

Record note (lead, round 5): this file is the lead's faithful condensation of the returned report, not the verbatim text that docs/review.md asks for; the full text was not retained. Rounds from 05 are stored verbatim.
