# Review round 4: docs-audit

Snapshot: c123394..85e635a on `docs/audit-2026-09-25`, clean tree. Lead
lenses: R3 items resolved; preferences.md as the single authority. All four
round-3 items resolved. AGENTS.md, git-workflow.md and development.md defer
to preferences.md; build-a-cv, new-cv skill, review.md, constitution.md,
pdf-workflow.md, history.md and the trello skill contradict it in neither
direction.

## Findings

### R4-F1 Minor: pdf-workflow.md still uses the retired "safety checkpoint" term
`pdf-workflow.md:138-140`, `constitution.md:4,12-13`. Point to
preferences.md; constitution says where the reserved operations are listed.

### R4-N1 Note: the exception's condition covers only the brand default
Name the word: "flagship" may appear in the candidate's own words once the
footer shows the brand.

### R4-N2 Note: merging makes git-workflow.md:23-24 false
The tracked `exports/*.pdf` carry "Marine CV" metadata while the code emits
"CVgen"; refreshing them needs the owner's go and no document says which
rule gives way; the release steps state that go last. Fix: rule under
Branches (record the mismatch on the handoff card until the go); move the
go to the top of "Releasing a new render".

### R4-N3 Note: one owner decision lives outside preferences.md
"Further rounds require an explicit owner decision" (review.md) is missing
from the "He decides" list.

## Checks rerun
`git diff --check` exit 0; CR bytes 0; link scan 19 links 0 problems;
baseline pins none of the changed files; pymupdf metadata read of
`exports/*.pdf`. Suite not rerun (markdown only).

## Verdict: PASS

## Disposition (lead, 2026-09-25)
All four applied: pdf-workflow.md and constitution.md point to
preferences.md; the exception names "flagship" only; git-workflow.md states
the exports rule under Branches and asks for the go before step 3 of a
release; preferences.md lists review rounds beyond the cap. The exports
metadata mismatch goes on the handoff card for the owner.
