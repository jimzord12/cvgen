# Review round 4: pdf-workflow (closes note N4; branch fix/status-wrong-shape)

Snapshot: branch `fix/status-wrong-shape`, head `45f008c`, base `main` = `3296e91`; one commit, 2 files (`packages/cv-workflow/cv_workflow/workspace.py`, `tests/workflow.py`). Working tree clean before and after the checks.
Lead lenses: 6 (failure handling), 5 (tests).
Coverage:
1. Wiring: `scripts/cv.py status` -> `workspace_status` -> `Revision.state()`; `workspace_status` hashes the on-disk `cv.pdf` and never reads `render.json` itself, so `state()` is the only JSON path in `status`. Proven by the real CLI on a disposable fixture: `render.json` = `[]` -> exit 0, `"state": "corrupt"`; restored -> `exported`.
2. Correctness: 24 shapes probed on both `main` and head. Head: list/string top level in any of the three records -> `corrupt`; `pdf` missing/`null`/`{}`/`[]`/int hash -> `corrupt`; `{"status":"failed"}` and the real failed record -> `failed`; good record with `cv.pdf` missing -> `changed`; genuinely failing or stale checks -> `checks-failed`; stale/foreign/empty approval -> `rendered`; untouched -> `exported`. No genuine state is swallowed.
3. Integrity: no write path added; `state()` read-only. The test's restore is byte-identical; step 8's approve/export of rid3 passing proves the restored record still binds to its bytes.
4. Contracts/privacy: no schema or record-shape change; nothing under `private/` touched.
5. Tests: the new step fails on `main` for each shape independently: `[]` -> `AttributeError`, `{"status":"success"}` -> `KeyError: 'pdf'` (cv.pdf exists in rid3), `pdf: null` -> `TypeError`; each a traceback, exit 1, caught by `expect=0`. The assert message names the shape.
6. Failure handling: `require_checks`/`require_approval` use `.get` only, and the pre-check makes every present record a dict before `describe()` runs; the export receipt path already maps a bad receipt to `export-conflict`. See M1.
7. Simplicity: the broad `except (KeyError, TypeError, AttributeError)` is defensible for a function documented "never a proof of anything"; a real bug in `describe()` would turn all seven asserted states in step 8 into `corrupt`, failing the suite.
8. Repository: both files LF; one Conventional Commit; no doc enumerates state names or workflow steps.

## Findings

### M1 Minor: N4's second half (approve/export traceback on a non-object record) is not covered
`render.json` = `[]` -> `approve`/`export` `AttributeError`, exit 1; `pdf: {"sha256": 5}` -> `TypeError`. Smallest fix: two lines in `read_json` (`must hold a JSON object`), covering all six read sites.
Disposition: fixed in the follow-up commit `ab1da95`; the `state()` loop reverted to plain `read_json(record)`; the workflow case also asserts `approve` refuses `[]` with that message; suite PASS `builds/tests-20260921-024012-723114`.

### N1 Note: three further shape changes beyond the tested ones, all improvements (`pdf.sha256` non-string, success without `pdf` and no `cv.pdf`, `checks.json` `errors: null`) now read `corrupt`.
### N2 Note (pre-existing, outside scope): an unreadable `cv.pdf` (OSError in `workspace_status`) and a dot-prefixed directory under `revisions/` still prevent a listing. Retained for triage.

## Checks rerun
- `python tests/run.py` at `45f008c`: exit 0, `PASS: 38 compilation cases`, 9 workflow checks -> `builds/tests-20260921-023508-815813/`.
- Isolated main-vs-head probe on a copy of the fixture -> `builds/review-status-wrong-shape-r1/probe-result.json`; real `scripts/cv.py status` on that copy. All records restored; tree clean.

## Limitations
Windows only; Linux CI not checked. Trello not read; N4's text taken from `reviews/03.md`. `private/` not opened.

## Verdict: PASS

No Blocking or Material finding. `status` reads `corrupt` for every wrong-shape record constructed, the three tested shapes each fail on `main`, and no genuine state is misreported. M1 fixed in the follow-up commit without a new round.
