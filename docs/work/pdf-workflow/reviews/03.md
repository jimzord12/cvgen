# Review round 3: pdf-workflow

Snapshot: `feat/pdf-workflow` head `ce3ac93` (whole change `4a3d6ad..ce3ac93`, 22 files; round-3 delta `f5ef0e1..ce3ac93`: `workspace.py`, `render.py`, `tests/workflow.py`, `reviews/02.md`). Working tree clean before and after my checks; no engine, example, export, baseline or CI file in the delta.
Lead lenses: 6 (failure handling and recovery), 5 (tests and visible evidence).
Coverage:
1. Wiring: `scripts/cv.py` -> `cv_workflow` unchanged; `tests/run.py` still drives `run_workflow`; the removed wrapper in `render.py:29` yields the identical `not valid JSON` message through `read_json`, so the existing render refusal assertion still binds.
2. Correctness: `state()` pre-parses only records that exist, then `describe()`; a missing export receipt is an `OSError` caught at `workspace.py:206` (`export-conflict`); `UnicodeDecodeError`/BOM are `ValueError` subclasses and become `corrupt` (probe E). Wrong-shape valid JSON left in N4.
3. Integrity: the delta adds no write path; `state()`/`read_json` are read-only; every probe left records untouched and restorable.
4. Contracts/privacy: unchanged; `--test-only` refusal under `private/` still asserted (`tests/workflow.py:70-72`); nothing created under `private/` by my runs.
5. Tests: step 7 (`tests/workflow.py:131-133`) fails for each half of F5 if reverted: without the `read_json` change `approve` tracebacks (exit 1 vs `expect=2`); with it but without the `state()` pre-parse, `require_checks` raises `WorkflowError` inside `describe()` and the state is `checks-failed`, not `corrupt`; `status` exit 0 on the same call proves the other revisions still list. Restoration (`:134`) is exact, so step 8 approves rid3 on the genuine record. My rerun: `exact/result.json` raster+text identical to v11.
6. Failure handling: F5 fix is real and complete on all four read paths, verified by probe: corrupt `render.json` (A), BOM `checks.json` (E), corrupt `cv.approval.json` (B) -> `REFUSED: <file> is not valid JSON ...`, exit 2, `status` exit 0 with `corrupt`; corrupt export receipt (C) -> `export-conflict` and `export` refused naming the file. N3 fix real: both git calls use `**TEXT` (`render.py:99-101`), no duplicate kwargs.
7. Simplicity: `describe()` split is the minimal way to distinguish a parse failure from a failed check; the double read is negligible; no scope growth.
8. Repository: all four changed files LF (`git ls-files --eol` and byte scan); one Conventional Commit; `reviews/02.md` stored with dispositions; `docs/work/<id>/task.md` is intentionally absent (`development.md:77`); no doc enumerates state names, so `corrupt` needs no doc change.

## Findings

### N4 Note: a record that is valid JSON but the wrong shape still tracebacks in `status` (hides the workspace) and, for a non-object top level, in `approve`/`export`
Anchor: `packages/cv-workflow/cv_workflow/workspace.py:187-190` (`describe()`), `:135-136` (`require_rendered`), `:46-50` (`read_json`).
Scenario (probe D, `builds/review-r3/probe.log`): `render.json` = `{"status": "success"}` -> `status` exit 1 `KeyError: 'pdf'` (approve correctly refuses "did not render successfully"); `render.json` = `[]` or `null` -> `status`, `approve` and `export` exit 1 `AttributeError: ... has no attribute 'get'`.
Expected: `corrupt`/refusal like F5. Impact: negligible; needs a deliberate hand edit of a machine-written record that keeps it parseable; nothing is written or released. Outside F5's stated scope, so optional and not counted against the verdict.
Smallest fix: in `read_json`, `if not isinstance(data, dict): raise WorkflowError(f'{path} must hold a JSON object')` (the check `render.py:30-31` already makes for `candidate.json`, which could then be dropped), and in `describe()` use the same guard as `require_rendered`: `if render.get('status') != 'success' or not render.get('pdf'): return 'failed'`.

## Checks rerun
- `python tests/run.py` at `ce3ac93`: exit 0, `PASS: 35 compilation cases`, 9 `workflow-*` cases; revisions record engine `ce3ac93`, `uncommitted_changes: false`; `workflow/exact/result.json` raster and text equal on both pages; `commands.log` contains the corrupt-`checks.json` refusal and `"state": "corrupt"` -> `C:\Users\jimzord12\Documents\GitHub\marine-engineer-cv\builds\tests-20260916-223123-329044\`.
- Probes A-E via `python scripts/cv.py render|approve|export|status` on `builds/review-r3/ws1/` (script in my scratchpad, log at `builds/review-r3/probe.log`); one render, all records restored; tree clean; nothing under `private/`.

## Evidence inspected
- Author's `builds/tests-20260916-222751-164262/`: `report.json` (35 PASS), `workflow/report.json`, `workflow/commands.log` (corrupt refusal names `checks.json`; status lists rid3 `corrupt` beside the others), `workflow/exact/result.json`. Its `render.json` records engine `f5ef0e1` (run 46 s before the commit); the engine is untouched by the delta and my rerun on `ce3ac93` reproduces it.
- `docs/work/pdf-workflow/reviews/01.md`, `02.md` with dispositions; sources at `ce3ac93`: all six package files, `scripts/cv.py`, `tests/workflow.py`, `tests/run.py`, `packages/cv-workflow/README.md`, `docs/development.md:61-81`, `docs/review.md`.

## Limitations
Windows/cp1252 only; the Linux CI run for `ce3ac93` was not checked (no external systems). No Trello access; task text taken from the brief. Private entry points not opened (out of scope).

## Verdict: PASS

F5 and N3 are fixed as described, complete across sibling paths, and covered by a test that fails for either half reverted; no Blocking, Material or Minor finding. N4 is optional.

---

## Dispositions (author, 2026-09-16)

- N4 Note: deferred. It needs a hand edit that keeps a machine-written record parseable; nothing is written or released in that case. Recorded on the card as a follow-up for the next workflow change (the guard belongs in `read_json` plus the `describe()` status test). The loop stops here at PASS; the closing snapshot is `ce3ac93`.
