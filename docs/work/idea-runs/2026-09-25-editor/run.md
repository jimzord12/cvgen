# Idea run: magazine-editor, 2026-09-25

First run of the `magazine-editor` agent, unattended overnight (round cap
10 per gate). The agent file was new this session, so it ran as a
`general-purpose` agent told to act exactly as
`.claude/agents/magazine-editor.md`; the reviewers ran the same way.

## Brief given

Date 2026-09-25; two or three concepts. Owner steer: the niche is custom,
unique, premium work; marine is the live field (fictional marine records in
`examples/candidates/`), travel and tourism is next, so at least one
concept should serve both fields with different data. The owner decides by
looking at the PDF.

## Concepts delivered

| Folder | Idea | Record | Fonts |
|---|---|---|---|
| `design-concepts/2026-09-25-measured-in-months/` | Career drawn to scale: every vessel a bar as long as its months on one ruler, then a timetable-style company ledger; travel variant | chief officer | Archivo (OFL) |
| `design-concepts/2026-09-25-feature-opener/` | CV as magazine feature: kicker, 112 pt serif headline name, italic standfirst, vessels as a contents list, months as a pull-quote; travel variant | captain | Instrument Serif, Newsreader (OFL) |
| `design-concepts/2026-09-25-fleet-in-signs/` | Count the fleet in signs, Isotype-style: one original ship sign per vessel, shape = class, colour = rank | engineer | Jost (OFL) |

Author's known limits: some references are secondary pages; variable-font
rendering not checked in print; black-and-white checked by greyscale render
only (rank also written in text on concept 3); travel data is an invented
sketch; concepts 1 and 3 need months for every vessel, concept 3 a vessel
class field.

## Rounds
- Research gate: PASS round 1 (reviews/01-research-reviewer.md); six Notes applied by the author.
- Design gate round 1: FINDINGS, one Blocking (measured-in-months strip order inside each employer) (reviews/02-design-reviewer.md).
- Design gate round 2: PASS, all three concepts (reviews/03-design-reviewer.md); its three Nits (D12-D14) applied by the author afterwards and checked visually by the lead.

## What reaches the owner

All three concepts, status `proposed`. Open for the owner: the engineer example record names a real institution in its education entry (D11); changing it touches the frozen v11 reference, so it is his call.
