# Review round 1: domains-move

Snapshot: branch `refactor/domains-move`, head `10fd66d`, base `main` = `b54d9f9` (= tag `archive/pre-domains`); one commit, 75 paths. Working tree clean before and after the checks.
Lead lenses: 3 (data and document integrity), 8 (repository and documentation).
Coverage:
1. Wiring: `lib.typ` -> `domains/marine/templates/flagship/*`; both artwork packs, all 8 fixtures, `tests/workflow.py` ENTRY (root-absolute), `build.ps1`, `verify.py` `FLAGSHIP` and the four examples all reach the moved files; proven by a rerun of the suite and four fresh compiles. Import depths read by hand: template 4 levels to `core/`, components 5, examples 3 to `packages/` and 2 to `candidates/`. `core/` still imports nothing from `domains/`. `typst.toml` `exclude` names the moved `tests` folder, which exists.
2. Correctness: no behaviour change; the only non-path edits are comments, `$id`/description strings and the baseline keys.
3. Integrity: all 18 binary renames (17 SVG + reference PDF) keep the same blob id at R100; baseline 31/31 keys, `sorted(values)` identical to `archive/pre-domains`, 18 entries re-keyed 1:1 by value, every key resolves on disk with the recorded SHA-256, `typst`/`renderer` fields unchanged. `examples/candidates/*` and `archive/design-studies/{0*,1*,shared.typ,content/*}` absent from the diff; the two example JSONs still pin `/examples/candidates/fictional-engineer.png`. 68 changed text files: no BOM, no CRLF, trailing LF present; SVGs are single-line (`i/none`, unchanged blobs).
4. Contracts: both schemas change only `$id` and description text; facts/input shape untouched.
5. Tests: a wrong asset path cannot pass silently: `decoration` does `read(asset.source)` (compile error on a missing file), backgrounds are read without defaults, `check_frozen()` hashes every baseline key (missing file raises). Both packs, the skills bullet and all four examples are compiled by the suite.
6. Failure handling: n/a, no runtime path changed.
7. Simplicity: scope matches the ADR 0011 Stage 1 list; `examples/candidates/` deliberately kept; no extra cleanup.
8. Repository: acceptance grep returns zero hits; broader greps (`templates/flagship` without the `domains/marine` prefix, `schema/candidate.schema.json` without `marine/`, `flagship/assets`) found the two doc leftovers below. Constitution section 1 names the new reference and example; section 3 cites `lib.typ`, which did not move. Commit message is a single Conventional Commit describing exactly this scope.

## Findings

### M1 Minor: two doc snippets keep a two-level relative depth for a folder that is now three levels deep
Anchor: `README.md:40-44`, `docs/reference/skills-component.md:8-10`, `docs/reference/layout-and-pagination.md:18`. An agent copying the README snippet into `examples/marine/flagship/<new>.typ` gets a loud file-not-found. Fix: README depth to `../../../` and `../../candidates/`; the two reference pages switch to root-absolute `/packages/cv-engine/...` as `build-a-cv.md`, `workflow.py` ENTRY and skill `new-cv` already prescribe.
Disposition: fixed in the follow-up commit on the branch before merge.

### M2 Minor: `architecture.md` still cites the removed `schema/` path in the inputs table
Anchor: `docs/architecture.md:28`. Fix: `domains/marine/schema/candidate.schema.json`.
Disposition: fixed.

### N1 Note (pre-existing): `README.md:70` says the suite compiles 26 cases; it runs 35.
Disposition: fixed in the same follow-up commit (one word).

### N2 Note (pre-existing, outside scope): `archive/design-studies/*.typ` reference `../assets/*.svg` and `../assets/fictional-engineer.png`, which have not existed since the 2026-09-16 monorepo move. Frozen, never compiled by the suite.
Disposition: recorded for triage; frozen files are not edited.

## Checks rerun
- `python tests/run.py` at `10fd66d`: exit 0, `PASS: 35 compilation cases`; `exact/result.json` raster and text equal on both pages -> `builds/tests-20260921-020334-553045/`.
- `typst compile` of the four examples, then `tests/verify.py --actual engineer.pdf --reference <moved v11 PDF>` -> passed -> `builds/review-domains-move-r1/`.
- Read-only script: baseline old/new comparison, on-disk hash resolution, blob-id check of the 18 binary renames, BOM/CRLF/trailing-newline scan of the 68 changed text files.
- `git grep` runs listed under Coverage 8; `git diff -M --summary`, `--raw`, `git ls-files --eol`.

## Evidence inspected
Author's `builds/tests-20260921-015810-230169/` (`report.json` 35 passed; `exact/result.json` equal; workflow `render.json` records engine commit `10fd66d`, `uncommitted_changes: false`) and `builds/library-20260921-015831-016/` (four PDFs). Sources at `10fd66d`: full diff of all 75 paths, `lib.typ`, `core/*.typ`, every moved `.typ`, `tests/{run,verify,workflow}.py`, `typst.toml`, `.github/workflows/verify.yml`, `AGENTS.md`, `docs/constitution.md`, `docs/architecture.md`, `docs/reference/*.md`, `docs/guides/build-a-cv.md`, `README.md`, the three skills, ADR 0011, `docs/review.md`.

## Limitations
Windows only; the Linux CI run for `10fd66d` not checked. Trello not read. `docs/pdf-workflow.md` target tree excluded per brief: it still presents the ADR 0010 layout as "the approved target", carried to the core-split card. `private/` not opened.

## Verdict: PASS

No Blocking or Material finding. Every frozen byte, hash and wiring site is carried 1:1 and the engineer example is pixel-identical to the moved reference on the rerun. M1 and M2 are two small documentation-path fixes landed in a follow-up commit without a new round.
