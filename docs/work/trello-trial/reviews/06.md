<!-- code-reviewer report, round 6, 2026-09-16, in-session Agent tool invocation. Stored unchanged; dispositions are in docs/proposals/trello-free-trial.md. -->

# Review round 6: trello-trial

Snapshot: `cddd379..6df9343` on `docs/product-direction`, HEAD = `6df9343fcb992a1ec44522786624703063223600`, working tree clean before and after my checks. Helper blob at HEAD `3adc440`; fake-credential probes ran on a byte-identical scratchpad copy (hash verified), live probes on the working-tree file. Full range `ec5c23d..6df9343` (seven commits) re-read for the closing pass.
Lead lenses: 4 (access and privacy) and 6 (failure handling)
Coverage:
- 1 Wiring: `AGENTS.md:112,128-130` route to the skill; SKILL.md -> `trello.ps1`; recipes via `& $T`; live `-Lists`/`-Cards` exit 0. The new "profile exports them" claim holds: user and machine scopes empty, `CurrentUserCurrentHost` profile mentions `TRELLO_API` (boolean check only), and child `pwsh` processes inherit the values.
- 2 Correctness: `\z` guard refuses LF-tailed key or token, CRLF, CR, leading/embedded LF, space, tab, quote, NBSP, empty and whitespace-only values (exit 2, `-ErrorVariable` empty, `$Error` 0); live key (32) and token (76) pass; `$?`/exit semantics unchanged. Unicode case-equivalents: T6-02.
- 3 Integrity: board unchanged (trial-a Done 3/3, trial-b Active 0/2, no labels); phone verdict recorded with date and attribution; no product data touched.
- 4 Access/privacy: T5-01 closed (LF path exits before any request). Class check on this snapshot: 400 and 404 live with `-ErrorVariable`: seeded caller record intact, target headers User-Agent + Accept-Encoding, 0/0 hits in plain, `Get-Error`, 3 KB `fl * -Force`; `-InformationVariable`/`-WarningVariable` empty on failure and success (new this round); `-Debug`, `$DebugPreference='Continue'`, `-Verbose` 60/60/221 bytes, 0 hits; `builds/` 1445 files and scratchpad 67 files, 0/0; tree: every 32+ hex run is a 40-char SHA; profile directory is not inside a git work tree. I consider the credential-exposure class closed for the helper as designed, including malformed values.
- 5 Tests/evidence: no automated test (accepted rounds 1-5); no author evidence path was given for round 6, so I reproduced every claim (see Limitations).
- 6 Failure handling: T5-02 closed: under caller `Set-StrictMode -Version Latest`, refused connection, HTTP 401, the `-Lists` pipeline and a real 30 s timeout (`TaskCanceledException`) all print the designed line, exit 1, `$Error` 0, 0 hits. One narrower strict-mode gap remains: T6-01.
- 7 Simplicity: three code lines plus wording; no scope creep.
- 8 Repo/docs: all ten files in the range LF; commit scope coherent; round-5 report stored with the same provenance comment and linked; dispositions T5-01..03 accurate against the code; `trial` is a recognised proposal state; profile wording consistent across helper, SKILL.md and proposal.

## Findings

### T6-01 Minor: caller strict mode still breaks the catch when an HTTP error has an empty body
Anchor: `C:\Users\jimzord12\Documents\GitHub\marine-engineer-cv\.claude\skills\trello\trello.ps1:78` (`$_.ErrorDetails.Message`).
Scenario: caller session under `Set-StrictMode -Version Latest`; Trello or an intermediary answers an error status with no body (e.g. a 502 from the edge). `Invoke-RestMethod` sets `ErrorDetails` only when the body is non-empty (verified against a local listener: empty 502 -> `HttpResponseException`, `ErrorDetails` null; with body -> set), so line 77 sets `$status` and line 78 throws `PropertyNotFoundException`.
Expected: designed line, exit 1, `$Error` 0. Actual: message and exit code lost, `$?` False, a PropertyNotFound record in the caller's `$Error`. No credential exposure: lines 74-75 have already removed the record and scrubbed the header. Same class as T5-02, narrower precondition; does not affect the verdict.
Fix (one token, verified under strict mode on both cases, same text): `$detail = if ($status) { [string]$_.ErrorDetails } else { ... }`. Evidence: `fake\emptybody-strict.txt`.

### T6-02 Note: the guard is case-insensitive, so Unicode case-equivalents of ASCII letters pass it
`-notmatch` ignores case; on fake values, KELVIN SIGN (U+212A) and U+0130 pass `^[A-Za-z0-9_-]+\z` (long s, dotless i, NBSP are refused). Outcome is safe: .NET refuses the header at send time with "Request headers must contain only ASCII characters", which does not quote the value; 0 hits in every holder, `$Error` 0. Only the label ("no response ...") and exit code (1, not 2) are off, and no realistic misconfiguration produces such a value. Optional one-character fix: `-cnotmatch`.

### T6-03 Note: a whitespace-only credential mangles the usage message
`TRELLO_API_TOKEN=' '` reaches `Fail`, whose redaction replaces every space in the message with `<redacted>` (`fake\ws-get-stderr.txt`). Exit 2, nothing leaks; cosmetic. Optional: skip redaction for secrets shorter than 8 characters.

## Checks rerun
Folder: `C:\Users\jimzord12\Documents\GitHub\marine-engineer-cv\builds\review-trello-r6-20260916-172847\` (119 files + `README.txt` index).
- Live, working-tree helper (blob `3adc440`), counts only, `live\summary.txt`: `-Lists`/`-Cards 'Marine CV trial'` exit 0 (`lists.json`, `cards.json`); `-Cards` inside a strict-mode scope exit 0; `GET boards/doesnotexist` (400) and `GET nonexistent-path-for-review-r6` (404) with `-ErrorVariable`; 400 under strict mode exit 1, `$?` False, seed intact; `GET members/me` exit 0 with `-ErrorVariable/-InformationVariable/-WarningVariable` all empty; `-Debug`, `$DebugPreference`, `-Verbose` captures; `builds/` sweep 1445 files 0/0. Counter sanity on planted values: key 1, token 2.
- Fake-credential child `pwsh -NoProfile` runs of the byte-identical copy, `fake\*-summary.txt` and `*-stderr.txt`: guard shapes (ok, lf, keylf, crlf, cr, leadlf, midlf, space, quote, tab, empty, ws, kelvin, longs, dotlessi, capdoti, nbsp); strict mode with refused proxy, HTTP 401, LF, Kelvin, `-Lists` pipeline, and a real 30 s timeout through a silent local TCP listener; empty-body 502 via a local `HttpListener` (`emptybody-strict.txt`).
- No POST/PUT/DELETE. CV suite not run: no product code changed (agreed).

## Evidence inspected
`git diff cddd379..6df9343`; `git log ec5c23d..6df9343`; `trello.ps1`, `SKILL.md`, `recipes.md`, `AGENTS.md`, `docs/proposals/trello-free-trial.md`, `docs/proposals/README.md:43-53`, `docs/work/trello-trial/reviews/01.md`-`05.md` at 6df9343; `docs/review.md`; `git ls-files --eol` (ten files, all LF); `git grep` for credential shapes at 6df9343; `git worktree list` (trial worktree still at c39ca1e); `git status` clean at 6df9343.

## Limitations
- No author evidence path was supplied for round 6; every claim was reproduced live or with fake values instead of inspecting author output.
- No live value was printed or written; live values were used only inside in-process regex counts. All `fake\` files hold the FAKE strings only.
- In the in-process strict-mode 400 probe, `-ErrorVariable` landed in the script-block scope, so its hit count is not meaningful; the fake strict probes (`ok-get-strict`, 0/0 hits) cover that holder.
- Scratchpad additions: `trello-r6.ps1`, `probe-r6-child.ps1`, `probe-r6-drive.ps1`, `probe-r6-emptybody.ps1`, `live-r6.ps1`, `r6-outdir.txt`. Nothing under the source tree changed.
- The credential rotation recorded in the proposal as the remaining step before the trial endpoint is an owner action outside this snapshot; this PASS does not close it.
- Write recipes accepted on rounds 1-5 evidence and read-back state.

## Verdict: PASS
