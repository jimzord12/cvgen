<!-- code-reviewer report, round 5, 2026-09-16, in-session Agent tool invocation. Stored unchanged; dispositions are in docs/proposals/trello-free-trial.md. -->

# Review round 5: trello-trial

Snapshot: `8d231af..cddd379` on `docs/product-direction`, HEAD = `cddd3795b1d9b8a5feeb72130031f8683f64039c`, working tree clean before and after my checks. Helper blob at HEAD `6dd4b05`; fake-credential probes ran on a byte-identical scratchpad copy (hash verified), live probes on the working-tree file. Full range `ec5c23d..cddd379` re-read for the closing pass.
Lead lenses: 4 (access and privacy) and 6 (failure handling)
Coverage:
- 1 Wiring: `AGENTS.md:128-130` routes to the skill; SKILL.md -> `trello.ps1`; recipes via `& $T`; live reads exit 0.
- 2 Correctness: `-Lists`/`-Cards`, query path, `$?` False + exit 1 after failure, True + 0 after success, all live. Live key (32 chars) and token (76) pass the guard. Guard anchor defect: T5-01.
- 3 Integrity: board unchanged (trial-a Done 3/3, trial-b Active 0/2, five lists); no product data touched.
- 4 Access/privacy: class statement below; one remaining path, T5-01.
- 5 Tests/evidence: no automated test (accepted rounds 1-4); every author claim reproduced live; the 30 s timeout branch exercised for the first time in any round.
- 6 Failure handling: exits 1/2/0 on every branch including a real timeout (clear message, 30 s); the guard refuses quote, space, CR, CRLF, `=` and empty before any request with `$ev` empty; caller strict mode breaks the no-response branch: T5-02.
- 7 Simplicity: two lines plus a guard loop; no scope creep.
- 8 Repo/docs: nine files LF; commit scope coherent; round-4 report stored with the same provenance comment; dispositions accurate except that T4-01's "validated before any request" has the T5-01 gap; SKILL.md:50-53 becomes accurate once T5-01 lands; scope wording: T5-03.

**Credential-exposure class, stated explicitly.** For well-formed credentials I consider it closed for the helper as designed. Evidence on this snapshot: on 400 and 404 (live) and 401, connection-refused and real timeout (fake), the caller is left with nothing that carries the header: `$Error` 0 with a seeded caller record intact; the `-ErrorVariable` record's `TargetObject` and `Exception.Response.RequestMessage` headers are User-Agent and Accept-Encoding only; `InvocationInfo.BoundParameters` is empty (my one new theory, refuted); 0 hits in plain print, `Get-Error`, `fl * -Force`, a depth-4 string walk, 12 MB CLIXML and 8.8 MB JSON serialisations; the timeout record has no request object at all; success leaves `-ErrorVariable`/`-InformationVariable`/`-WarningVariable` empty; debug/verbose/information paths unchanged since rounds 2-4. The one path left needs a malformed value (T5-01); after its one-character fix I would call the class closed. Out of the helper's scope, as accepted in T3-02: diagnostics a caller enables on purpose session-wide (`Set-PSDebug -Trace`, `Trace-Command`, a forced `*:SessionVariable` default) and reading `$env:` directly; anyone who can do those already has the values.

## Findings

### T5-01 Material: a credential with a trailing LF passes the new guard; the .NET FormatException then hands the raw header to `-ErrorVariable`
Anchor: `C:\Users\jimzord12\Documents\GitHub\marine-engineer-cv\.claude\skills\trello\trello.ps1:49` (`'^[A-Za-z0-9_-]+$'`); `SKILL.md:50-53`; disposition T4-01.
Scenario: `TRELLO_API_TOKEN` (or the key) set from a file or clipboard read with `-Raw` that ends in LF (this repo's editors write LF; the owner is about to set rotated values), then `& $T GET cards/<id> -ErrorVariable ev; $ev`, the step SKILL.md now invites.
Expected: exit 2 before any request and an empty `$ev`, as for quote, space, CR and CRLF.
Actual (fake values, `fake\nl-lf-*`): .NET `$` matches before a final newline, so the guard passes (`regexpass=True`); `Invoke-RestMethod` throws `FormatException` whose message quotes the whole header; the helper exits 1 with a redacted, mislabelled "no response..." line; `$Error` is clean, but `$ev` holds the record and plain `$ev` prints key and token 4 times each (`Get-Error` 4/4). CRLF and CR tails are refused correctly (exit 2, `$ev` empty).
Impact: the exact T4-01 sub-path the regex was added to close, reachable through an ordinary misconfiguration; same class as T2-01, T3-01, T4-01. The precondition is narrower (the helper never works in that state), which the lead may weigh; not softened.
Fix (one character, verified on scratch copy `trello-r5-patched.ps1`): `'^[A-Za-z0-9_-]+\z'`. LF-tailed value then exits 2 with `$ev` empty and `$Error` 0; live `-Lists` still exit 0; both live values pass `\z` (`fake\patched-lf-summary.txt`, `live\summary.txt`).

### T5-02 Minor: caller `Set-StrictMode` breaks the no-response branch (after the scrub)
Anchor: `trello.ps1:76`. With `Set-StrictMode -Version Latest` in the caller (inherited by the script scope), `$_.Exception.Response` on `HttpRequestException`/`TaskCanceledException` throws `PropertyNotFoundException`: the designed message and exit code are lost, `$?` False, a PropertyNotFound record lands in `$Error`. HTTP-error path unaffected (401 under strict mode: message, exit 1, `$Error` 0). The scrub at :75 has already run: 0 hits in every strict dump (`fake\strict-*`, 47 KB `Get-Error`). Fix: `$status = if ($_.Exception -is [Microsoft.PowerShell.Commands.HttpResponseException]) { $_.Exception.Response.StatusCode.value__ }`.

### T5-03 Note: credential scope wording
`SKILL.md:14` and proposal line 135 say user-scope variables. In my tool session both values are present in the process environment but `[Environment]::GetEnvironmentVariable(..., 'User')` is empty, so they come from another scope (machine or profile). Harmless to the helper (`$env:`), but "set at user scope, start a new shell" may mislead after rotation. Confirm against the owner's setup; one-word doc fix if so.

## Checks rerun
Folder: `C:\Users\jimzord12\Documents\GitHub\marine-engineer-cv\builds\review-trello-r5-20260916-170850\` (58 files).
- Live, working-tree helper (blob `6dd4b05`), counts only, `live\summary.txt`: `-Lists`/`-Cards 'Marine CV trial'` exit 0 (`lists.json`, `cards.json`); `GET members/me` with `-ErrorVariable/-InformationVariable/-WarningVariable` all 0; `GET boards/doesnotexist` (400) and `GET nonexistent-path-for-review-r5` (404) with `-ErrorVariable`: exit 1, `$?` False, `$Error` 0, target headers User-Agent + Accept-Encoding, 0/0 hits in plain, `Get-Error`, `fl * -Force`, walk, headers and 12 MB CLIXML; `builds/` sweep 1324 files, 0 with key, 0 with token.
- Fake-credential child `pwsh` runs of the byte-identical copy, `fake\`: 401 (`http-*`), connection refused (`refused-*`), real timeout through a silent local TCP proxy (`timeout-*`: 30 s, exit 1, `TaskCanceledException`, null target), strict mode (`strict-*`), serialisation (`ser-*`: CLIXML 11.5 MB, JSON 8.8 MB, walk), validation shapes (`validation.txt`, `nl-*`), patched copy (`patched-lf-summary.txt`). Hit counter sanity on planted values: key 1, token 2 (`sanity\planted.txt`).
- No POST/PUT/DELETE. CV suite not run: no product code changed (agreed).

## Evidence inspected
`git diff 8d231af..cddd379`; `git diff --stat ec5c23d..cddd379`; `trello.ps1`, `SKILL.md`, `recipes.md`, `AGENTS.md:112,128-130`, `docs/proposals/trello-free-trial.md`, `docs/work/trello-trial/reviews/01.md`-`04.md` at cddd379; `docs/review.md`; `git ls-files --eol` (nine files, all LF); `git status` clean at cddd379.

## Limitations
- No live value was printed or written; live values were used only inside in-process regex counts. All `fake\` files hold the FAKE strings only.
- T5-01 demonstrated with fake values; the live values are well-formed, so nothing was exposed this round and no rotation is needed for it.
- The scratchpad is shared with the lead session; I added `trello-r5.ps1`, `trello-r5-patched.ps1`, `probe-r5.ps1`, `count-r5.ps1`, `ser-r5.ps1`, `val-r5.ps1`, `nl-r5.ps1`, `live-r5.ps1`, `r5-outdir.txt`. Nothing under the source tree changed.
- Write recipes accepted on rounds 1-4 evidence and read-back state.

## Verdict: FINDINGS
