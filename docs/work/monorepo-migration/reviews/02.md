<!-- code-reviewer report, round 2, 2026-09-16, in-session Agent tool invocation. Stored unchanged; dispositions are on the Trello card https://trello.com/c/Z0CTAVrT. -->

# Review round 2: monorepo-migration

Snapshot: fe9bd1d..2f76890 on feat/monorepo-migration (HEAD 2f76890, clean tree, in sync with origin; one commit, docs/skills/config/fixture/PNG only, no engine code). Closing pass over e72d46a..2f76890. Trello card 6aaaceb60486a30209511735 read (credentials loaded from the profile, not printed); dispositions under "## Review" match the diff.
Lead lenses: 8 (repository and documentation), 5 (tests and evidence)
Coverage:
1. Wiring: four entry points, scripts/build.ps1, tests/run.py + verify.py (ENGINE/FONTS/FLAGSHIP constants), CI workflow, AGENTS.md and README commands all use the packages/cv-engine tree; CI run 35133648432 success on 2f76890 (gh, read-only).
2. Correctness: no code change this round; my suite rerun repeats round 1's chief-officer, hidden-duration and negative cases.
3. Integrity: exact gate raster and text equal on both pages in my rerun; manifest 31 entries, check_frozen first and last; exports untouched in 2f76890; Chief Officer previews pixel-identical to a fresh 96 dpi render of the committed export.
4. Contracts/privacy: three records validate against both schemas; a record with `copy` is rejected by the facts schema and accepted by the Flagship input schema (rerun); private/ last written 2026-09-12; nothing private or credential-like in the diff.
5. Tests/evidence: see m2 closure below; previews are the reviewed revision (m1 closed).
6. Failure handling: unchanged; the two residual stale `/assets/` doc paths fail loudly (file not found), not silently.
7. Ownership/scope: the commit does exactly what the dispositions say; no widening.
8. Repository/docs: AGENTS.md, README, architecture, guide, references, skills, .gitignore, .gitattributes, typst.toml, archive README coherent; LF clean on every changed file. Residual leftovers m4-m6.

M1 closed: candidate-schema.md drops `copy` from the facts example and the new "Flagship wording" section states defaults in adapter.typ, key-wise merge, order defaults < record < argument, matching adapter.typ:17-19 and flagship.typ:17. M2 closed at all listed anchors except one (m4); no silent path remains: every `--font-path` in active docs/skills/scripts is `packages/cv-engine/fonts`.
m2 closed and demonstrated: the tightened assertion (data.typ:7-8) fails against a swapped merge, the old one did not (isolated case, real defaults and record, only the merge line copied and swapped).

## Findings

### m4 Minor: two `/assets/...` root-path statements survived (one is a round-1 M2 anchor)
Anchor: docs/reference/artwork-pack.md:34 (`source | Path from the project root, /assets/...`); docs/conventions.md:39 (`Paths from the project root for assets: /assets/...`)
Both are backtick-quoted, which the author's `"/assets/` grep could not see. A developer following either gets a loud Typst file-not-found and the correct path sits in the code block above (artwork-pack.md:9-15). Fix: replace with `/packages/cv-engine/templates/flagship/assets/...`. Optional tidy: conventions.md:51-53 still lists `copy` among the "Candidate JSON" keys; it does point to the Flagship schema, so leave or reword to "Flagship input adds `copy`".

### m5 Minor: "three PDFs" still in git-workflow
Anchor: docs/git-workflow.md:19 and :90. Same class as the .gitignore fix in m3; now four deliverables.

### m6 Minor: governance status lines say the migration is pending
Anchor: docs/constitution.md:7-11 ("Migration is pending"); docs/vision.md:15-17 ("Implementation is pending... migration sequencing are the next planning topics"). Both files already use the migrated paths and AGENTS.md:118 says the engine part is implemented; a reader orienting from vision.md would think the move has not started. Fix: one sentence each ("engine migrated 2026-09-16; workflow package pending"). vision.md is owner-owned, so record a deferral if the owner should confirm the wording.

### N2 Note: older preview PNGs differ in size
docs/images for engineer and both captains (committed 2026-09-12) are 778x1100; the new Chief Officer pair is 794x1123 at 96 dpi. Pre-existing, harmless at the README's 30% width; re-render all four pairs together at the next release.

### N3 Note: "an example under `examples/`"
.claude/skills/new-theme/SKILL.md:21 and docs/reference/artwork-pack.md:56; new-cv SKILL.md:18 has the precise `examples/flagship/<name>.typ`.

## Checks rerun
- `python tests/run.py` -> exit 0, PASS 26 cases -> builds/tests-20260916-212132-339749 (exact/result.json: p1/p2 raster and text equal; chief-officer-copy.log and engineer.log empty, no font warnings)
- `typst compile --root . --font-path packages/cv-engine/fonts` on two isolated cases -> builds/review-r2-20260916-212231: swapped-merge.typ (new assertion vs swapped adapter) exit 1 with `experience: "Record heading"` vs `"Sea service"`; swapped-merge-old-assertion.typ (round-1 assertion) exit 0
- Scratchpad scripts: preview-vs-export pixel comparison, jsonschema validation of three records against both schemas, CRLF scan of the changed files; `gh run list`, `git ls-files --eol`, tag and merge-base checks

## Evidence inspected
- builds/tests-20260916-211803-660151/report.json (author run, 21:18:08, one minute before the commit; PASS 26, exact gate equal)
- docs/images/Marine-Chief-Officer-CV-Silver-v01-page-{1,2}.png and exports/Marine-Chief-Officer-CV-Silver-v01.pdf at 2f76890
- docs/work/monorepo-migration/reviews/01.md at 2f76890; Trello card desc and Acceptance checklist (8/9 complete, last item awaits this round); CI runs 35133648432 and 35131041159

## Limitations
private/ inspected by metadata only. Linux CI relied on the GitHub run status. The swapped-merge demonstration copies the adapter's merge line into a scratch file rather than mutating the real adapter, per the no-edit rule. Round-1 lenses 1-7 were re-covered through the suite rerun, the schema rerun and reading, not repeated in full depth.

## Verdict: PASS
No Blocking or Material findings remain. m4-m6 are one-line documentation fixes the author can apply or defer with a reason; they do not touch the implementation and would not need another round on their own.
