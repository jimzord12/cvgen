# Review round 1: domains-adr

Snapshot: branch `docs/adr-0011-domains`, briefed head `d9ac08f` over base `753e809` (main): nine files, all under `docs/`. During the review the branch advanced to `00b034e` (one hunk, ADR 0011 lines 77-79: the owner-approval sentence now names the section 6/7 rewording "explicitly" and the repository rename). Both examined; engine, examples and tests are byte-identical across `753e809..00b034e`. Working tree clean before and after the checks.
Lead lenses: 8 (repository and documentation), 7 (simplicity and ownership).
Coverage:
1. Wiring/product fit: ADR 0011's Context describes today's code accurately, verified: `packages/cv-engine/core/data.typ:7-30,52-78` models companies > groups > ships with rank and months, `:54` requires `identity.rank`; `schema/candidate.schema.json` requires `identity.name`, `identity.rank`, `companies` with `additionalProperties: false`; `docs/framework-gaps.md` holds exactly three open entries; portraits are pinned as `/examples/candidates/fictional-engineer.png`; `tests/baseline.json` is path -> SHA-256, so "regenerated with identical values" is the right description of a move. The Decision section is target state written in the present tense; the Consequences (four stages, "never ahead of the code") say which is which, except one parenthetical (m1). Vision no longer says maritime-only anywhere.
2. Correctness: n/a, no rendering or data behaviour changed; suite rerun green as a guard.
3. Integrity: verified by diff: 0002 lines 4-6, 0007 lines 4-7 and 0010 lines 8-9 are all inside the Status paragraph, `## Context` onward untouched. History entry is a new trailing section in the file's style, nothing above it changed. No frozen file, fixture or export touched.
4. Contracts/privacy: the stated target contract (core requires only `identity.name`; marine keeps `rank` and companies/groups/ships) contradicts nothing in the constitution or ADR 0002/0007 as amended. The merge order domain < role < template < record `copy` < call-site `copy` extends today's `flagship-copy + facts.copy + copy` (`adapter/adapter.typ:19`) without reordering existing precedence. Pattern scan of every added line: no name, e-mail, number, credential or `private/` path.
5. Tests/evidence: n/a for tests; rerun below.
6. Failure handling: n/a.
7. Simplicity/ownership: scope stayed governance-only. The ADR authorises only the settled decisions and records the owner's approvals (ADR 0011:77-79); the constitution's "in writing" condition is met by that record. Section 6: "Calendar periods are never converted into service time." present verbatim, "computed once from the full candidate" kept, so section 6 is unchanged in force. Section 7: "No template is forked by role" became "unless a recorded reason makes it role-level", a deliberate, owner-approved relaxation that the settled hierarchy requires; vision principle 2 and conventions "No role branches" say the same. Deliverable rule `<Domain>-<Role>-CV-<Variant>-vNN.pdf` fits the four exports and the frozen file exactly as the old rule did.
8. Repository/docs: `git ls-files --eol`: `i/lf w/lf` for all nine; byte scan: 0 CR, no BOM, trailing newline. Conventional Commits. ADR 0011 carries the README template headings. README row present. `conventions.md:47` (`/packages/cv-engine/templates/flagship/assets/...`) names today's tree and is a move-stage update, acceptable now (N1).

## Findings

### m1 Minor: ADR 0011 names `core/node.typ`, `merge` and `compose` as if they exist
Anchor: `docs/decisions/0011-domains-roles-templates.md:41-44`. Nothing by those names exists; the only merge today is the shallow `+` at `adapter/adapter.typ:19`. Fix: say the helper is added in the core-split stage and that today the adapter does a shallow `+`.
Disposition: fixed in `docs/decisions/0011-domains-roles-templates.md` before merge.

### m2 Minor: the three framework gaps now point at the wrong roadmap item
Anchor: `docs/framework-gaps.md:23,35,42` ("roadmap item one"); the vision renumbered, deck data support is item 3. Fix: "open, roadmap item three (deck data support, marine domain)".
Disposition: fixed.

### m3 Minor: the history entry overstates in two places
(i) "generalised without weakening them" is true for section 6 only; section 7 relaxed an absolute to a default with a recorded-reason exception. (ii) "Four independent review rounds on the plan" are not recorded in the repository. Fix: describe the two rules precisely and drop or locate the plan-review sentence.
Disposition: fixed; the plan reviews were held in the planning session outside the repository, the sentence is dropped.

### N1 Note: conventions mix today's tree with ADR 0011's future markers
Acceptable now under the ADR's stage-by-stage rule. Disposition: the Candidate JSON split goes on the `domains-core-split` card.

### N2 Note: AGENTS.md still describes the old scope
Disposition: a one-line pointer to ADR 0011 added to the first paragraph; the map itself changes with the move stage.

### N3 Note: for the `domains-core-split` card
`core/pagination.typ:4-9` (`selection`) counts rows from `company.groups...ships`; the core-split stage needs a domain-supplied row count. Also pre-existing: `docs/constitution.md:10-11` still says "the workflow package is pending". Disposition: the first is already in the core-split plan (model with `count`); the second is fixed in the move stage's constitution path update.

## Checks rerun
- `python tests/run.py` at `00b034e`: exit 0, `PASS: 35 compilation cases`; `report.json` `passed: true`, `exact_reference.passed: true` -> `builds/tests-20260921-015002-430709/`.
- `git ls-files --eol` plus a byte scan of the nine files and of the diff: LF, 0 CR, no BOM.

## Limitations
No Trello access: card state taken from the brief. Owner approvals taken from the ADR's record and the brief, not observed. Windows only.

## Verdict: PASS

No Blocking or Material finding. Three Minor items fixed by the author before merge; three Notes dispositioned above. The PASS belongs to `d9ac08f`; `00b034e` changes one sentence that only strengthens the approval record.
