# Review round 2: glossary

Snapshot: `8f7bf35..c66551c` on `docs/glossary` (HEAD c66551c, clean tree), read with `git show`/`git diff`. The focus was `810c6c8..c66551c`: commits 76360b2 and c66551c.
Lead lenses: 8 (repository and documentation), 7 (simplicity and ownership)
Coverage:
1. Wiring: the rule reaches the lead (AGENTS.md:173-178), Codex, ceo (33-34, 94-97), magazine-editor (36, 120-123) and code-reviewer (review.md:63). All six owner decisions in the brief appear. Gaps: m4.
2. Correctness: I checked every code-name cell at c66551c. All paths exist. `identity.rank` is in candidate.schema.json:16 and data.typ:39. Only `domains/marine` exists, and `exports/` holds four PDFs. `core/` imports no domain and holds the asserts behind "checks". Unbuilt rows say "not built". `Framework` and `Envelope` describe only what exists.
3. n/a: no data, revision, hash or approval path changed.
4. Tool lines are unchanged. Write limits are intact (ceo.md:17, 82; magazine-editor.md:19-21), and both agents hand terms to the lead. No private data.
5. n/a: only Markdown changed, and `tests/` reads none.
6. n/a: there is no runtime path.
7. There is one glossary. The rule is restated in five places for different readers; that is justified, but drift has already started (m4). The review exemption grew beyond the owner's instruction (m5).
8. All 9 files have LF endings, 0 CR and a trailing newline. Dispositions are in the 76360b2 body. Round 1:
   - M1 is properly superseded: terms left "He decides" and joined "Agents decide", and AGENTS.md:206 now agrees.
   - M2, m1 (in touched files), m2, m3 and N1-N3 are resolved.
   - See m6 and N4-N6.

## Findings
### m4 Minor: two places are out of step with the new rule
Anchor: AGENTS.md:132; docs/conventions.md:106-108
- AGENTS.md:132 still points agents to the glossary for "spotting new candidates", which is the superseded flow. The c66551c body says the candidate wording was removed. Glossary line 68 says "candidate" in prose means `Client`, so the always-loaded map now literally says "spotting new clients".
- conventions.md gives only the "add" half. An agent writing docs from it adds a term and reports nothing. AGENTS.md:177-178 covers agents that read it, so the impact is low.

Fix:
- AGENTS.md:132: "…backticks in replies, adding terms and reporting them".
- conventions.md: append "…and named in the next report (glossary Rule 4)".

### m5 Minor: the review exemption widened just as the owner's check went away
Anchor: docs/review.md:16-19; c66551c body
Scenario: the next branch builds `Envelope` drawers and updates those rows. Those edits skip the independent round. The owner no longer confirms rows, and he does not read paths. So a row that shows unbuilt work as built, the defect round 1 found (M2) in `Framework`, gets only the author's check.
Expected: the owner removed his approval, nothing more. In 76360b2 the exemption covered new Candidates rows, justified as "a proposal, not a rule".
Actual:
- At head it covers official rows "added, renamed or dropped", and under Rule 1 those are rules.
- The commit body calls this "same rule, candidate wording removed".
- The idea-run exception (review.md:21-25) records who adopted it and why; this one records nothing.

For context: proposals/README.md:66 says an applied rule is changed through a new linked proposal. The working precedent here is that provenance note.
Impact: none on this branch; the first row edit after the merge is exposed. This is a judgment, not a present defect.
Fix:
- Define the focused check for a term row: "code name matches the tree at that commit; anything unbuilt says 'not built'".
- Add provenance: adopted by the lead on 2026-09-25 under the owner's "as little friction as possible".

### m6 Minor: the "say instead" table is not applied where this branch edits, and no follow-up covers it
Anchor: docs/glossary.md:62-68; AGENTS.md:4; docs/preferences.md:44; .claude/agents/ceo.md:47, 72, 83; .claude/agents/magazine-editor.md:3
Scenario:
- ceo.md:33 tells the ceo to write in official terms, but its own brief says "what candidates pay for CVs" (47) and "a real candidate" (83).
- AGENTS.md:4, edited here, keeps "The engine is a domain-neutral core plus domains". That is the whole-package sense, which glossary line 65 says to call "the package".
- "He decides" says "a real candidate's PDF" (preferences.md:44).
- magazine-editor.md:3 says "Never touches the engine".

Expected: as with round-1 m1, touched lines either follow the glossary or the sweep is recorded.
Actual: the recorded follow-up (76360b2 body) covers only field -> domain in three files. The "candidate" row is also unqualified, unlike "'role' for a job title". So it also catches candidate ideas (ceo.md:72, "the candidates you considered").
Fix:
- Reword the row as '"candidate" for the person'.
- Either fix these lines now or add the table's words to the recorded follow-up.
- Optional: one line in Rule 3 saying that a rename or drop updates the prose using the term in the same change, or records the sweep.

### N4 Note: `CVgen` has two definitions in text agents always read
Anchor: docs/glossary.md:33; AGENTS.md:3; docs/decisions/0011-domains-roles-templates.md:1; docs/vision.md:21
The row says only vision.md still says "library". AGENTS.md:3, edited here, and the ADR 0011 title say "library" too. What the product is, is the owner's call (preferences.md:43). Name AGENTS.md in the row, and queue the wording for his go on the handoff card.

### N5 Note: two more words with more than one meaning (proposed rows)
- "release": `Release` means the public PDFs, but git-workflow.md:96 and AGENTS.md:82 use the word for package version tags.
- "approved": line 64 gives two meanings. A third is the proposal state (proposals/README.md:45) and "an approved item" (preferences.md:46-47). Suggested row: keep "approved" for owner-approved proposals and designs.

### N6 Note: terms from undecided ideas
Anchor: .claude/skills/idea-run/SKILL.md:67-69; docs/glossary.md:17-23
Step 8 adds every term an author returns, including terms for pending proposals and proposed concepts. None of Rule 3's triggers covers a first coinage for an idea nobody has decided on. The row would read "not built", exactly like agreed work such as `Scout`, and it would linger after a rejection. Fix: set the code name to "pending: <proposal path>" and drop the row under Rule 5 if the idea is rejected.

## Checks rerun
None; nothing was written under `builds/`. Read-only checks:
- `git ls-files --eol`, plus a CR and trailing-byte count on the 9 files
- `git cat-file -t` on every code-name path
- `git diff --name-only 8f7bf35 c66551c`: Markdown only
- `git grep` of `tests/` for Markdown references: none

## Evidence inspected
At c66551c:
- the 9 changed files, the 76360b2 and c66551c bodies, and the per-commit diffs from 810c6c8
- `lib.typ`, `core/*.typ`, and the marine `data.typ` and `candidate.schema.json`
- docs: development.md, proposals/README.md, proposals/development-protocol.md (status `applied`), constitution.md, git-workflow.md, guides/build-a-cv.md, framework-gaps.md, decisions/0011, vision.md
- listings of `exports/`, `domains/` and `docs/work/idea-runs/`

## Limitations
- The owner's decisions exist only in conversation. I took the brief's list as given and could not verify details such as "rechecked after 6 months".
- Trello is out of reach. I could not verify the field -> domain follow-up, or that the owner was told of the 20 agent-added terms (Rule 4).
- I could not compare 01.md with the text originally returned.
- Reachability was judged from instruction files. Agents started before c66551c still run on the old text.

## Verdict: PASS
There are no Blocking or Material findings. m4-m6 are for the author to fix or to defer with a recorded reason.
