<!-- code-reviewer report, round 1, 2026-09-16, in-session Agent tool invocation. Stored unchanged; dispositions are in docs/proposals/trello-free-trial.md ("Owner decision and application"). -->

# Review round 1: trello-adoption

Snapshot: `f03597d..7e2eb2f` (one commit, HEAD `7e2eb2f`, branch `docs/product-direction`, working tree clean; `main` and both remotes still at `f03597d`, so the commit is local only). Files: `AGENTS.md`, `docs/development.md`, `docs/proposals/trello-free-trial.md`, `.claude/skills/trello/SKILL.md`, `recipes.md`. Helper `trello.ps1` unchanged, as the brief states.
Lead lenses: 8 (repository and documentation), 1 (product fit and wiring)
Coverage:
1. Wiring: traced `CLAUDE.md -> AGENTS.md:23-28,129-132 -> SKILL.md:37-43 -> board` live. `-Cards 'Marine CV'` lists five cards; `GET cards/6aaaceb6...` returns the migration card with nine incomplete acceptance items, links to ADR 0010 / pdf-workflow.md / development.md on `main` (all present on `main`). Orientation works.
2. Correctness: helper matches board name with `-eq` and demands exactly one open board (`trello.ps1:85-87`), so the shorter name `Marine CV` is unambiguous. n/a otherwise, no product code.
3. Document integrity: proposal status `pending -> applied` with a dated decision entry; earlier approved text preserved as README requires. One splice leftover (A1).
4. Contracts/privacy: cards contain plan text only, no credentials or real candidate data; helper output saved under `builds/` holds no secrets. Credentials in my shell were stale (401), loaded per the brief without printing.
5. Evidence: author checks (rename, five cards) reproduced live; see Checks rerun.
6. Failure handling: AGENTS.md now says "if the board cannot be read, say so"; SKILL.md documents the stale-shell 401 path I actually hit and its remedy.
7. Ownership/scope: workflow rules moved to `development.md`, mechanics stayed in the skill; no helper change; Blocked convention placement (A3).
8. Repo/docs: five files LF, `git diff --check` clean, commit message matches the diff. Active docs (AGENTS, development, review, skill, proposal) agree that the card is the record; the only `task.md` references left are historical (A4).

## Findings

### A1 Minor: dangling "Recommendation so far" fragment inside the decision section
Anchor: `docs/proposals/trello-free-trial.md:274-276`
Scenario: the old sentence "Owner action outstanding: the adoption decision." was replaced by the new section, but its continuation "Recommendation so far: adopt for the migration task; the Free tier covered..." survived, glued to the last bullet. Expected: the decision section states the decision only (the recommendation already lives at :243). Actual: a bullet reads "The skill's trial-scope wording is removed. Recommendation so far: adopt..." after adoption. Impact: the authoritative decision record reads as if the decision were still open. Fix: end the bullet after "removed."

### A2 Minor: card template keeps a `Status:` line that duplicates the list
Anchor: `docs/development.md:83-85` vs `:69-72`
Scenario: a fresh agent creates a card and copies the template. Expected: status is the list plus the Blocked label, one home. Actual: template starts with `Status: Queued | Active | ...`, a second ledger to keep in sync (the three real cards already omit it). Fix: drop the line or replace it with "Status: the list and the Blocked label".

### A3 Minor: Blocked convention has no active rule home
Anchor: `docs/development.md:71-72`; `.claude/skills/trello/SKILL.md:9-10`; `recipes.md:38-40`
Scenario: SKILL.md sends readers to `development.md` for workflow rules; the reason / dependency-link / unblock-condition section and "label comes off only when the blocker is resolved" now exist only in `recipes.md` (mechanics, loaded on demand) and the applied proposal (:72-73). Fix: one sentence at `development.md:72`.

### A4 Minor: applied development-protocol still says adoption is pending
Anchor: `docs/proposals/development-protocol.md:31-32,44-46,99`
Scenario: "permanent task-tool adoption still needs a decision" and "Without a task tool, use `docs/work/<id>/task.md`" with no pointer to the adoption. README forbids rewriting approved content, and its metadata is `applied`, so orientation will not surface a phantom decision; still the last `task.md`-as-default reference in the tree. Fix: append one dated line under "Authorized trial amendment" pointing at the proposal's "Owner decision and application". Optionally the same for `work-records.md:16-19,38`.

### A5 Note: fictional `trial-b` sits in Active (0/2)
Anchor: board, card `6aaa8e4b...`; `SKILL.md:71-72`
A fresh orientation must consciously exclude it from "Now" (one active task). The skill's "leave them alone" sentence covers it; moving or archiving is cleanup under the owner's confirmation checkpoint.

### A6 Note: report persistence
Brief says "store nothing"; `docs/review.md:158-160` and the AGENTS.md working agreement expect `docs/work/<id>/reviews/NN.md` linked from the record. Lead's disposition; `docs/work/trello-adoption/reviews/01.md` plus a link from the proposal's decision section would match how the trial rounds were kept.

### A7 Note: wording
`.claude/agents/code-reviewer.md:16` "the task record path" (now a card URL); `docs/development.md:33-34` "the active task record" could name the board as AGENTS.md does. The card's "Working rules" link serves the pre-adoption `development.md` until `7e2eb2f` reaches `main`.

## Checks rerun
- `./.claude/skills/trello/trello.ps1 -Lists 'Marine CV'`: exit 0, board `IPsBxAwf`, lists Queued/Active/Review/Ready/Done.
- `-Cards 'Marine CV' -Pretty`: exit 0, five cards (monorepo-migration Queued 0/9; pdf-workflow Queued, Blocked, 0/5; trello-cli Queued; trial-b Active 0/2; trial-a Done 3/3). Saved: `C:\Users\jimzord12\Documents\GitHub\marine-engineer-cv\builds\review-trello-adoption-r1-20260916-201932\cards.json`.
- `GET cards/<id>` with checklists and attachments for the three real cards: exit 0; descriptions match the proposal's claims (nine items; `## Blocked` section, red label, "Depends on monorepo-migration" attachment; reminder card says "not authorised work" with four triggers). Saved as `card-<id>.json` in the same folder.
- `git ls-files --eol` (five files LF), `git diff --check` (clean), `git cat-file -e main:<path>` for the six card-linked files (all present).
GET only; no POST/PUT/DELETE.

## Evidence inspected
At `7e2eb2f`: the five changed files in full; `docs/review.md`, `docs/proposals/README.md`, `development-protocol.md`, `work-records.md`, `kanban-tooling-research.md`, `docs/git-workflow.md`, `.claude/agents/code-reviewer.md`, `docs/work/dev-setup/task.md`, `trello.ps1:84-106`; `git diff f03597d..7e2eb2f`; live board state at 20:19 local.

## Limitations
Docs and board only; no CV suite run (no product code changed). Board state is a point-in-time read.

## Verdict: PASS
