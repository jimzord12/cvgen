# Review round 2: candidate-validation

> Record note (lead): condensed by the lead from the returned report; the original text was not retained. Round 03 is stored verbatim.

Snapshot: `feat/candidate-validation` at 94d1e65 on round-1 head e1614c7;
full range `422f150..94d1e65` rechecked. Lead lenses: R1-01..R1-03
resolution including the lib.typ-only rule; do the new assertions catch the
defects. Both private records resolve to `flagship-input` with 0 errors (in
memory, nothing written or quoted); neither private entry is lib-only. The
lib-only rule refuses no legitimate marine composition: `flagship-input` is
the most lenient marine contract.

## Findings

### R2-01 Minor: package README contradicts the code
`packages/cv-workflow/README.md:52-54` still says lib.typ-only entries are
not schema-checked. Fix one clause.

### R2-02 Minor: R1-03 fixed for key errors only
`validate.py:38,42`. `issued: 2021`, `review: null` or `["A","B","C",5]` in
a certificate still echo the object; "leave the key out" is wrong advice
for required keys. Fix: drop only alternative-level type mismatches
(`not (e.validator == 'type' and not e.relative_path)`); neutral null
wording; add `issued: 2021` to the suite's typo record.

### R2-N1 Note: lib.typ fallback keys on the import path, not the names
(a) lib.typ plus a marine role with no template resolves to the domain
schema, stricter than lib-only; (b) lib.typ plus a later domain without a
schema would be checked against Flagship. Suggest: Flagship fallback only
when every named domain is marine; add `validate.py` to the new-field
wiring checklist.

### R2-N2 Note: the checkout-location fix is not pinned by a test
Reverting `.relative_to(ENGINE)` keeps the suite green; optional test with
schemas copied under a `templates` folder.

## Checks rerun
`python tests/run.py` at 94d1e65: PASS 40, frozen v11 exact
(`builds/tests-20260925-015904-084772`). In memory: `schema_for` on 12
import sets, refusal text for 12 mutations.

## Limitations
Trello not read; missing-jsonschema path not exercised; private render not
run. Integration: expect a conflict with the docs-audit branch on the
build-a-cv `pymupdf` sentence; its section 3 exit-2 list lacks the schema
refusal.

## Verdict: PASS

## Disposition (lead, 2026-09-25)
All four applied in the next commit: README clause; alternative-level
filter and neutral null wording with `issued: 2021` and the null message
asserted; Flagship fallback only when every named domain is marine (lib +
marine role now resolves to Flagship, a non-marine domain never does);
`schema_for(imports, engine=...)` lets the suite copy the schemas under a
`templates` folder and assert the template schema still wins. Suite PASS 40
at that commit (`builds/tests-20260925-020318-139219`). The wiring-checklist
line and R1-04 docs land with the merge of the docs-audit branch.
