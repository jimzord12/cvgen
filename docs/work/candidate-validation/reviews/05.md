# Review round 5: candidate-validation

Snapshot: `feat/candidate-validation` at 5beff14, clean tree. I examined `d430f48..5beff14`: 77584b9 (the fixes: 6 files, +20/-12) and 5beff14 (stores `04.md`). Anchors are relative to `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-validation`.

Lead lenses: the brief named none, so I took them from its scope:
- (5) the R4-01 and R4-N2 fixes catch the defects they target;
- (6) both failures happen early and say what is wrong.

Coverage:
1. **Wiring:** the `cv.py render` path is unchanged. The `validate.py` edit only touches the docstring. `main` runs the new checks right after `check_frozen` (`tests/run.py:104-105`).
2. **Correctness:** all four real entries contribute a literal record (P0). `checked` is filled only inside the path loop, so an entry without a literal path fails (P2).
3. **Integrity:** n/a. No data path, schema or record changed.
4. **Contracts and privacy:** no contract change. I did not open `private/`. My suite run used an archive copy, so the `private/` path the workflow step refuses was the copy's own.
5. **Tests:** each fix fails its broken variant and passes on the real tree. Controls at d430f48 reproduce both round-4 defects.
6. **Failure handling:** a missing package now stops at `run.py:25`. An outdated package still gets the wrong label (R5-N1).
7. **Ownership:** the changes are small and local, and the scope has not grown.
8. **Repo and docs:** all 7 files are LF, `diff --check` is clean and both commit subjects are under 72 characters. The R4-N1 wording matches `schema_for`, and no other doc repeats "imports neither". The R4-N3 items are fixed.

## Findings

No Blocking or Material findings. R4-01, R4-N1, R4-N2 and R4-N3 are resolved.

### R5-N1 Note: an outdated jsonschema still reads as a broken record
Anchor: `tests/run.py:25`, `packages/cv-workflow/cv_workflow/validate.py:64-67`.
- **Scenario:** jsonschema is installed but older than 4.0, so it has no `Draft202012Validator`. P4 simulates this with a stand-in module.
- **Actual:** `AssertionError: example record ../../candidates/captain-example.json (read by captain-silver.typ) breaks its schema: the candidate check needs jsonschema: pip install jsonschema`. `pip install jsonschema` would then report the package as already installed.
- **Impact:** low. CI pins 4.26.0.
- **Fix:** at line 25, import `Draft202012Validator` from jsonschema instead of the bare module. Optionally, the refusal could ask for "jsonschema 4 or later".

### R5-N2 Note: skill wording
Anchor: `.claude/skills/verify-cv/SKILL.md:32-36`. The sentence "Fix the record (or the schema…)" now comes after the `ModuleNotFoundError` sentence, so it reads as advice for the missing-package case. Put the dependency sentence last, or give it its own bullet.

## Checks rerun
All output is in `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-validation\builds\review-r5-candidate-validation-20260925-033528\`: the driver `probe_r5.py`, the summary `probes.txt` and one transcript per probe. Probes ran on `git archive` copies with Python bytecode writing off. Temp files went to a folder inside it, which is empty afterwards.
- **P0:**
  - `check_example_records` passes on the 5beff14 copy, and the imported modules came from the copy.
  - The full `tests/run.py` on the same copy: exit 0, PASS 40, exact v11 match, engine 5beff14 with `uncommitted_changes` false. Output: `src-5beff14\builds\tests-20260925-033614-195143`.
- **P1:** jsonschema is blocked by an import hook that raises `No module named 'jsonschema'`.
  - At 5beff14: exit 1 at `run.py:25` with `ModuleNotFoundError`, and the output contains no "breaks its schema".
  - The d430f48 control gives the old mislabelled `AssertionError`.
- **P2:** `chief-officer.typ` loads its record through `#let record-path`, and the record has a misspelt key (`certficates`).
  - Sanity: `validate_record` refuses the record, yet Typst still compiles the entry (exit 0).
  - At 5beff14 the suite exits 1 at `run.py:44` with `AssertionError: no record found in: ['chief-officer.typ']`, before any compile.
  - The d430f48 control passes silently.
- **P4:** backs R5-N1.

## Evidence inspected
- The author's run `C:\Users\jimzord12\Documents\GitHub\cvgen-wt-validation\builds\tests-20260925-032730-233078`:
  - passed, 40 cases;
  - its render records show engine 5beff14 with no uncommitted changes;
  - written 15 s after the commit.
- The diff, both commit messages, `ls-files --eol` and `tests/baseline.json` (the chief-officer files are not frozen).
- At 5beff14: `tests/run.py`, `validate.py`, `render.py`, `workspace.py`, `approve.py`, the four changed docs and `04.md`.
- The toolchain matches the CI pins: Python 3.11.15, jsonschema 4.26.0, pymupdf 1.28.2, Typst 0.15.1.

## Limitations
- I did not read the Trello card (external), so the card update in the disposition is not verified.
- The merge commit that is meant to name the evidence folder does not exist yet. The 77584b9 message names only the command.
- My copy's engine record comes from the worktree's git, because the copy sits in the ignored `builds/`. That is equivalent here, since the worktree is clean at 5beff14.
- P1 and P4 simulate the package states inside Python; I uninstalled nothing.
- I did not fetch, and I did not see CI for 5beff14.

## Verdict: PASS

## Disposition (lead, 2026-09-25)
- R5-N1: the suite imports `Draft202012Validator` itself, so a pre-4.0
  package fails at the start; the render refusal asks for jsonschema 4 or
  later.
- R5-N2: the dependency failure has its own bullet in verify-cv, after the
  broken-record one.
- Both are note-level wording/import changes; no further review round (the
  verdict is PASS and the fixes are covered by the suite run named in the
  merge commit).
