# Review round 1: candidate-validation

Snapshot: `feat/candidate-validation`, range `422f150..e1614c7` (ea69911
code, tests, CI; e1614c7 docs). Lead lenses: correctness and failure
behaviour of the refusal path; test logic. Coverage: all eight lenses; both
private records resolve to `flagship-input.schema.json` and validate with
0 errors (checked in memory, nothing written or quoted).

## Findings

### R1-01 Minor: an entry that names no domain or template renders unchecked, silently
Anchor: `validate.py:33-35`, `render.py:88-96`. An entry importing only
`lib.typ` (build-a-cv section 8) gets `schema: null`; the summary does not
say so and a typo is dropped as before. Latent: no example or private
workspace takes this path. Fix: show `schema` in the summary; refuse or
default to `flagship-input` when lib.typ is the only engine import; test it.

### R1-02 Minor: template-over-domain priority depends on the checkout location, and no test pins it
Anchor: `validate.py:25`; `tests/run.py:33`; `tests/workflow.py:14-20`. A
checkout under a folder named `templates` makes both paths contain
`templates`; `max` then returns the first found, the domain schema for the
role-first import order, and a record with `copy` is refused. Replacing
`max` with "first found" leaves the suite green. Fix: key on
`relative_to(ENGINE).parts`; assert each example resolves to
`flagship-input`.

### R1-03 Minor: oneOf refusals do not name the misspelt key; values appear as Python reprs
Anchor: `validate.py:41-43`. A certificate with `reviw` reports the whole
object "is not valid under any of the given schemas"; `"months": null`
reports `None is not of type 'integer'`. Fix: unpack `context` sub-errors
whose validator is not `type`.

### R1-04 Minor: docs left stale
`README.md:73` (suite dependencies, 38 cases), `docs/architecture.md:121-127`
(module list lacks `validate.py`), `docs/reference/verification.md:55-67`
(example-record check and schema refusal missing),
`docs/reference/candidate-schema.md` error table (render-time refusal).

### N1 Note: `"months": 8.0` passes the schema and fails in the engine
JSON Schema `integer` accepts 8.0; the engine needs an int. Loud failure;
recorded for triage.

### N2 Note: verdict vocabulary is FINDINGS, not CHANGES (docs/review.md).

## Checks rerun
`python tests/run.py` at e1614c7: exit 0, 40 cases, frozen v11 raster and
text equal. `builds/tests-20260925-014700-426831` in the worktree. In-memory
refusal texts for seven typo cases and `schema_for` edge cases.

## Limitations
Trello card not read (external); missing-jsonschema refusal not exercised;
`cv.py render` not run on private workspaces (would write there).

## Verdict: PASS

## Disposition (lead, 2026-09-25)
- R1-01 fixed: summary carries `schema`; lib.typ-only entries are checked
  against the Flagship input schema (lib.typ's flat exports are marine and
  Flagship; later domains are prefixed); `None` only when no engine import.
- R1-02 fixed: priority judged on the path inside the engine; suite asserts
  every example resolves to `flagship-input` and pins role-first order,
  lib-only and no-import cases.
- R1-03 fixed: oneOf failures list the alternatives' non-type reasons; null
  says "leave the key out"; the object is no longer echoed. Tested.
- R1-04: fixed after the docs-audit branch lands on main, to avoid
  conflicting edits in the same files.
- N1: deferred; the engine refuses it loudly. Tightening would need
  `multipleOf` or a custom check; not worth it now.
