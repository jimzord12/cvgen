# Review round 1: framework-split

Snapshot: `C:\Users\jimzord12\Documents\GitHub\cvgen-framework-split`, branch refactor/framework-split, `git diff deee97e..4a2555f` (9f4206b code/paths, 4a2555f docs + ADR). HEAD 4a2555fecf9c. The worktree was clean before and after my checks.
Lead lenses: 3 (data and document integrity), 8 (repository and documentation)
Coverage:
1. Wiring: the examples, the `cv.py render` path (the workflow cases) and `build.ps1` all reach `packages/domains/marine/lib.typ`. The Framework's lib.typ exports 17 core names and nothing else. One gap in the one-off guide (F4).
2. Correctness: nothing renders differently. All four examples match `exports/`. I probed `schema_for` with relative imports, mixed imports and old paths: relative marine lib → Flagship; Framework lib plus a Flagship file → Flagship; Framework lib plus marine data → marine candidate schema; old cv-engine lib → None.
3. Integrity (lead): the v11 PDF and every pinned file moved byte-identical (42 R100 renames). Baseline hashes are unchanged and the keys map 1:1. Approve, export and status never recompile and never read `engine.package`, so old revisions can still be approved and exported. See F1 and F2.
4. Contracts and privacy: the schemas changed only `$id` and description. Nothing `$ref`s them and no record carries `$schema`. Rewritten entries get the same schema as before, because the rewrite maps each old path to its new one. The diff contains no private identifier. See N3.
5. Tests: the boundary check was extended, and isolated mutations show it rejects violations. The Framework-only lib is pinned to no schema. The legacy count covers both libs (9 + 23 = 32). See N4 and N5.
6. Failure handling: an entry that was not rewritten gets no schema, then Typst reports "file not found" and a failed revision is kept. The failure is loud. Boundary messages name the file and the target.
7. Simplicity: git mv, a path rewrite and two thin libs. The wrappers stay and no domain was added. Retiring the prefixed-names rule is recorded as a consequence in the ADR.
8. Repository (lead): no live `packages/cv-engine` path is left. `git grep` finds it only in history records and in ADR 0012's own text. LF endings and `.gitattributes` are fine. The ADR is in the index, the 0001/0008/0010/0011 statuses are amended, and no other branch uses the number 0012. See F3, N1 and N6.

## Findings

### R1-F1 Minor: the ADR and history say the private rewrite is done, but it exists only in a scratch script
Anchor: docs/decisions/0012-framework-and-domain-packages.md:53-58; docs/history.md:237-243; scratchpad split.py step 3.
- Scenario: the branch merges and step 3 is skipped or late.
- Expected: the real `private/*/cv.typ` files are rewritten together with the merge.
- Actual: at 4a2555f they are not rewritten, yet both documents say they "changed" and "stayed pixel-identical".
- Impact: `scripts/cv.py render` on either CV leaves a failed revision (`/packages/cv-engine/lib.typ` not found), and the records overstate the state.
- Fix: put the merge step on the card. After the rewrite, compile the real files into a new builds/ folder and compare them with reference.pdf, as private_check.py does. Also grep the workspaces outside `revisions/` for `cv-engine` (for example README build notes).

### R1-F2 Minor: the change to the render.json record is not recorded
Anchor: packages/cv-workflow/cv_workflow/render.py:102-109; ADR 0012, Consequences.
- Old revisions record `engine.package: "packages/cv-engine"`; new ones record `engine.packages: [...]`.
- Nothing reads either key today (only `commit` and `uncommitted_changes` are read). The future web backend will read records from both before and after the split.
- The schema `$id`s changed as well.
- Fix: add one line to the ADR's Consequences, or keep a `package` key.

### R1-F3 Minor: the mechanical rewrite left four sentences in current docs wrong or imprecise
- docs/pdf-workflow.md:92 says the entry point imports `/packages/cv-framework/...`. Marine entries import `/packages/domains/marine/...` (tests/workflow.py ENTRY, build-a-cv.md:34).
- docs/pdf-workflow.md:6 lists only `packages/cv-framework/` as the implemented engine and leaves out `packages/domains/`.
- docs/conventions.md:36 says a domain reaches the core "through `../cv-framework/core/`". No domain file can use that prefix: data.typ uses `../../`, Flagship components use `../../../../../`.
- packages/cv-workflow/README.md:50-55 says "when `lib.typ` is imported". Only marine's lib.typ triggers this now; the Framework's alone gives `schema: null` (validate.py:11, run.py:64).
- Fix: reword the four sentences.

### R1-F4 Minor: the guide for a one-off with no domain still says to import marine
Anchor: docs/guides/build-a-cv.md:227 (section 8), reached from client-workflow.md:183-186. domains-and-roles.md:87-89 says step 9 documents the Framework import; it does not.
- Scenario: a Travel & Tourism client, the case ADR 0012 cites. new-cv follows step 9 to section 8 and imports `/packages/domains/marine/lib.typ`.
- Result: `schema_for` picks Flagship's schema and render refuses the non-marine record. The new-cv skill says the opposite.
- Fix: add one sentence to section 8: with no domain, import `/packages/cv-framework/lib.typ`, and render then checks no schema.

### R1-N1 Note: "imports or reads" overclaims
ADR 0012:34. At render time, `decoration` in core/primitives.typ reads the domain's SVGs through paths the caller passes in. The suite proves only that no Framework code names a domain path (run.py:26-29); verification.md item 9 words this correctly. Say "names".

### R1-N2 Note: the chief officer's asset path is within the spirit of the owner's OK
- The owner's OK named import lines. The compass-bullet source is the same kind of engine path, changes nothing visible, and without it that CV stops compiling.
- preferences.md:55-56 lets agents edit a workspace's cv.typ.
- Tell him in one line. split.py's comment "import lines only" is inaccurate: its replace runs over the whole file.

### R1-N3 Note: copies of real client data under builds/
Going by the script's design, builds/private-check-20260925-133214 and -133239 in the split worktree hold copies of real records and portraits. They are ignored by Git, so not public. Clear them by path once the result is recorded.

### R1-N4 Note: the engine record is untested
Nothing asserts `engine.packages`. No case shows that a dirty file under packages/domains sets `uncommitted_changes`; this gap already existed for the single engine folder.

### R1-N5 Note: optional end-to-end proof
A fixture that imports only `/packages/cv-framework/lib.typ`, compiled with `--deps` and checked for any `packages/domains/` file, would prove the outcome directly.

### R1-N6 Note: small doc and glossary items
- architecture.md:16 makes paths relative to `packages/`, but line 146 starts with `packages/`.
- constitution.md:8-14 still says "both moves".
- The new history entry also narrates the client workflow, which is on the base branch under separate review.
- "one-off design" keeps appearing where the glossary says a one-off `Template`; "surface" for lib.typ also recurs without a glossary row.
- Out of scope (base branch): README.md:74 says 43 cases; the suite runs 44.

## Checks rerun
- `python tests/run.py` in the split worktree at 4a2555f: exit 0, "PASS: 44 compilation cases", builds/tests-20260925-134538-401616. Engineer pages 1-2 match v11 in raster and text.
- `scripts/build.ps1`: exit 0, builds/library-20260925-134816-541. All four PDFs are raster- and text-identical at 144 dpi to `exports/`.
- baseline.json, deee97e against 4a2555f: 31 entries with the same hashes. All 31 files on disk match.
- `check_core_boundary` run on temporary copies (in the session scratchpad, deleted afterwards): 6 violations rejected, 2 legitimate forms accepted.
- Names exported by the old lib.typ against marine's lib.typ: 51 of 51 identical.

## Evidence inspected
- The full diff of both commits.
- cv_workflow (render, validate, workspace, approve, export, `__init__`, README).
- tests/run.py, verify.py, workflow.py and the fixtures.
- Both lib.typ files and typst.toml.
- ADR 0012, the status lines of 0001/0008/0010/0011, and the decisions index.
- The live docs listed above, and `.github/workflows/verify.yml`.
- The author's builds/tests-20260925-133655-101991/report.json.
- private_check.py and split.py. I did not open `private/`.

## Limitations
- The Trello card was not read.
- Pixel identity of the two private CVs is author-run only. I checked the method, and the evidence folder dates from after the last code commit. I did not open it (client data).
- I could not verify that each reference.pdf is "approved", as the ADR says.
- I did not check the CI result.

## Verdict: PASS
