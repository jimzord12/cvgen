# Review round 2: framework-split

Snapshot: worktree `C:\Users\jimzord12\Documents\GitHub\cvgen-framework-split`, branch refactor/framework-split, `git diff ccc379b..b8b29db` (39629a2, c4408d5, b8b29db). HEAD b8b29dbb9e15. The tree was clean before and after my checks. I ran `git range-diff deee97e..4a2555f ccc379b..c4408d5`: both patches match round 1 except for context lines (glossary, build-a-cv, client-workflow, pdf-workflow, verification). The rebase lost nothing.
Lead lenses: 1 (product fit and wiring), 8 (repository and documentation)
Coverage:
1. Wiring (lead): yes, a client with no `Domain` can go end to end. I sent a fictional one-off that imports only the Framework through the real `scripts/cv.py render`. Result: schema null, success, checks passed, portrait snapshotted, and the snapshot reads no file under packages/domains. Approve and export read only records and hashes (approve.py, export.py), so no domain is involved. The steps around the new sentence still describe marine only (R2-F1).
2. Correctness: the schema choice is unchanged since round 1, and the suite pins "Framework lib.typ gives no schema". In my rerun the engineer example still matches v11.
3. Integrity: renamed files differ only in import paths and schema `$id`/description paths. Baseline hashes are unchanged; only the keys moved. ADR 0012 and history now reflect the F1 and F2 fixes. The private rewrite remains a merge step.
4. Privacy: the diff holds only placeholders and fictional names. F9, F11 and F15 close the client-workflow items. I did not open builds/private-check-*; the owner still has to clear them.
5. Tests: the suite passes. No case renders a Framework-only entry through cv.py (R2-N3).
6. Failure handling: an entry that imports any Flagship file gets Flagship's schema, so a tour-guide record is refused loudly. An entry that was not rewritten fails with "file not found".
7. Simplicity: b8b29db changes documentation only. Carrying client-workflow F9-F16 here is explained in the commit body.
8. Repository (lead): all 73 changed text files are LF, end in a newline and have no BOM; `git diff --check` is clean. `cv-engine` remains only in history records. The ADR index and status lines agree. The new term `Surface` must be named to the owner (glossary Rule 4). origin still holds the commits from before the rebase, so a force-push of this feature branch is owed.

Dispositions:
- Round 1: F1-F4 are fixed. N1 was applied only in the ADR (R2-N1). N6 is mostly applied (R2-N2). N2-N5 are fine; I raise N5 again as R2-N3 because this branch now sends real clients down that path.
- Client-workflow round 2: F9, F10, F11, F13, F15 and F16 are fixed. F12 is still broken (R2-F2). F14 is done; see R2-N4.

## Findings
### R2-F1 Minor: the no-domain path is one clause; the steps around it still describe marine only
Anchor: .claude/skills/new-cv/SKILL.md:28-35; docs/guides/build-a-cv.md:231; docs/constitution.md:50-53
- Scenario: the tour-guide client from guide section 9 is handed to new-cv.
- Actual:
  - new-cv step 1 says to copy a marine example.
  - Step 2 says "The record is validated for you" against Flagship's schema, but render checks no schema for this entry.
  - build-a-cv section 8 says "Keep the candidate JSON valid against the schema".
  - The constitution says each private entry point imports `packages/domains/marine/lib.typ`.
- Impact is low: the record goes unchecked while the checklist says it is checked. A photo not kept in `identity.portrait` is never copied into the `Revision`. Failures are loud, and the owner reviews the PDF before `Approval`.
- Fix:
  - new-cv step 2, one clause: with only the Framework no schema runs; keep `identity.name` and `identity.portrait`, and run `normalize-common` and `validate-common`.
  - build-a-cv: "its schema, if it has one".
  - Constitution: "its `Domain`'s `lib.typ`, or the `Framework`'s for a one-off `Template`".

### R2-F2 Minor: client-workflow F12 is still broken
Anchor: docs/review.md:20-22
- Actual: the old "after a focused check." survives at the end of the provenance sentence (`...under the owner's "as little friction as possible" after a focused check.`).
- Fix: delete it and end the sentence after the quote.

### R2-N1 Note: "imports or reads" remains in two live docs
docs/architecture.md:101 and docs/reference/domains-and-roles.md:55. The Framework's `decoration` does read domain SVGs at render time. Use the ADR's word: "names".

### R2-N2 Note: "one-off design"
- ADR 0012 lines 20, 32 and 45, and history.md:240, say "design" where the glossary says a one-off `Template`.
- ADR 0010's "Amended by 0012" line sits above its "Amended by 0011" line.

### R2-N3 Note: no suite case for the path real clients now take
My probe below is a ready-made fictional case for tests/workflow.py: render with `--pages 1`, then assert success and `schema is None`.

### R2-N4 Note: the three-month reminder has no trigger
client-workflow.md:218-220 and new-client step 9. Nothing reads the intake dates, and a client who never consents never reaches step 9. Move the rule to the skill's "Report to the owner" section or to the handoff card.

## Checks rerun
- `PYTHONDONTWRITEBYTECODE=1 python -B tests/run.py`: exit 0, "PASS: 44 compilation cases", `C:\Users\jimzord12\Documents\GitHub\cvgen-framework-split\builds\tests-20260925-140505-742160`.
- Probe, fictional, written only under `builds\review-r2-framework-split-20260925-140605\`:
  - `python -B scripts/cv.py render …/oneoff --pages 1`: exit 0, revision 20260925-110620-d9d435, schema null, checks passed, engine commit b8b29db, `uncommitted_changes` false.
  - `typst compile --deps` of that snapshot: exit 0. Its inputs are the Framework's lib.typ, 7 core files and the snapshot's own 3 files.
  - I did not run approve or export.
- Read-only: range-diff, `git diff --check`, the EOL/BOM scan, and greps for `cv-engine`, `private/` and ADR 0012 on the other branches.

## Evidence inspected
At b8b29db:
- The full diff and all three commit bodies.
- cv_workflow (validate, render, workspace, checks, approve, export) and cv.py.
- run.py, verify.py and workflow.py; both lib.typ files and the core files.
- The two guides; the new-cv, new-client and verify-cv skills; research-reviewer.
- ADR 0012 and the ADR index; glossary, framework-gaps, constitution, architecture, conventions, domains-and-roles, pdf-workflow, verification, the cv-workflow README and history.
- Both earlier reports.
- The author's report.json (passed, 44 cases) and builds/oneoff-proof.

## Limitations
- I did not read Trello (the merge step, acceptance criteria and dispositions on the cards).
- The private compositions' pixel identity is author-run and still to be checked at merge.
- I did not check CI.

## Verdict: PASS
